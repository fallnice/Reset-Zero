# 开发日志 · 背包·角色·战斗 统一游戏框架

> Unity 2022.3.57f1 LTS ｜ 背包 + 制造 → 开放世界角色游戏
> 定位：毕业设计 / 求职 Demo，商业级品质标准

## 日志使用约定

- 每天工作结束后，在下方「按日期日志」里追加一节，记录当天做了什么。
- Unity 侧的事（拖拽配置 / Play Mode 验证 / 手感调参）写成当天的「**待办（Unity 侧）**」，做完后在「**验证结果**」里回应：
  哪些跑通了、哪些要改、新踩了什么坑。**本日志两台机器（家 / 公司）共用**，所以一律用这套机器无关的小节名
  ——不写依赖「当前在哪台机器」的说法（早先那套小节名已废弃）：在哪台就写哪台能做的，做不了的写成待办。
- 复制文末「每日记录模板」即可快速开新的一天。
- 本文件为纯 Markdown，CodeBuddy 可直接读取，也可被规则引用。

---

## 第一部分 · 开发大纲

### 1.1 项目概述

本项目基于 Unity 2022.3 LTS 搭建，以背包/制造系统为起点，逐步扩展为包含角色控制、战斗、技能、骑乘、耕种等系统的开放世界角色游戏。采用 MVC + EventBus + 分层状态机架构，以商业级代码标准为目标，兼顾求职 Demo 展示与毕设答辩。

### 1.2 已完成的子系统

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 背包系统 (BagController + BagView) | 完成 | MVC 架构，格子管理，物品增删，容量显示 |
| 制造系统 (CraftController + CraftView) | 完成 | 配方数据库（SQLite），材料消耗，制造数量 |
| 数据库层 (SqliteManager) | 完成 | SQLite 持久化，DAO 层封装 |
| 事件总线 (EventBus) | 完成 | 全局发布订阅，错误隔离，调试模式 |
| 输入抽象 (IInventory) | 完成 | 为联机/多人预留的接口抽象 |
| UI 管理器 (UIManager) | 完成 | 面板栈管理，强制关闭支持 |
| 角色系统 (CharacterRoot + 状态机) | 完成 | 代码位移，分层状态机，CharacterConfig 配置 |
| 交互系统 (IInteractable + Detector) | 完成 | OverlapSphere 检测，拾取/提示 UI；武器掉落物进专属栏，全局浮动提示 |

### 1.3 核心架构决策

**角色状态机：分层状态 + 协调器分发**

采用「分层状态机 + 协调器分发」模式。全身状态机负责位移（Idle/Walk/Run/Jump/Fall），上半身状态机通过 Animator Layer 叠加处理瞄准/射击等动作。CharacterStateCoordinator 统一管理特殊状态（死亡/眩晕/过场），各子控制器实现 IStateResponder 自行决定响应策略。

**配置抽离：CharacterConfig (ScriptableObject)**

所有角色数值（walkSpeed/runSpeed/jumpForce/gravity/rotationSpeed 等）抽离为独立 ScriptableObject 资产。CharacterRoot 只持有引用不存数值，各 State 通过 `character.Config.xxx` 访问。纯数据与行为分离，不同角色可复用/覆盖 Config。

**输入抽象：IInputProvider**

玩家输入和 AI 输入通过 IInputProvider 接口统一，角色状态机只依赖接口，不关心输入来源。便于后续实现敌人 AI 复用角色控制逻辑。

**数据共享：Blackboard + EventBus 互补**

- Blackboard：持久状态数据池（IsGrounded、CurrentWeapon、Health 等），子控制器主动拉取。
- EventBus：瞬时事件通知（OnJump、OnHit、Interaction_TargetChanged 等），模块被动响应。

**交互系统：IInteractable 接口 + OverlapSphere 检测**

IInteractable 接口在独立 namespace，世界物体（PickupItem 等）实现接口，不依赖 Role 系统。InteractionDetector 挂玩家子物体，用 `Physics.OverlapSphereNonAlloc` 降频检测（0.1 秒一次），通过 EventBus 广播提示文本给 UI。PickupItem 通过 `GameRoot.Instance.Inventory` 获取 IInventory 接口入背包。

### 1.4 待实现的子系统

| 模块 | 优先级 | 说明 |
| --- | --- | --- |
| 战斗系统 | 逻辑层完成 | 近战/枪械行为、攻击输入、冷却、攻速、全/半自动与命中完善已接；敌人受伤/动画待做 |
| 装备系统 (EquipmentController) | 逻辑层完成 | 固定三槽、地图掉落物拾取（同槽拒绝）、每槽冷却、全/半自动已接；模型挂载待做 |
| UpperBody 状态机 | 接口层完成 | Inactive/RangedReady + 瞬时动作语义；Animator 适配器待资源对接 |
| IK 控制器 | 待实现 | 手脚 IK、LookAt 瞄准、武器姿态适配 |
| 表情系统 (ExpressionController) | 待实现 | 面部表情混合，动画驱动 |
| 音频系统 (AudioController) | 待实现 | Footstep 检测、武器音效、环境音效 |
| 敌人 AI | 规划中 | 行为树/状态机，复用 IInputProvider |
| 技能系统 | 规划中 | 技能冷却、Buff/Debuff |
| 任务系统 | 暂缓 | 已确认可延后，架构预留接口 |
| 多人联机 | 暂缓 | IInventory 接口已预留，可后续接入 |

---

## 第二部分 · 按日期日志

### 2026-07-01｜角色系统架构搭建

本日核心工作是完成角色系统的完整架构设计与代码骨架实现，为后续角色移动、战斗、AI 等模块奠定基础。

**文件夹结构**

在 `Assets/Scripts/Role/` 下建立以下目录：

- `Core/` — 核心基础设施（IInputProvider、Blackboard、IStateResponder、CharacterStateCoordinator、CharacterConfig）
- `Input/` — 输入层（PlayerInputProvider + PlayerInputActions）
- `StateMachine/` — 状态机（BaseStateMachine、FullBodyStateMachine、UpperBodyStateMachine）
- `States/FullBody/` — 全身状态（Idle、Walk、Run、Jump、Fall）
- `States/UpperBody/` — 上半身状态（预留）
- `Controllers/` — 子控制器（Equipment、IK、Expression、Audio）
- `Interaction/` — 交互检测器（InteractionDetector）
- `CharacterRoot.cs` — 角色根节点（统筹所有模块）

**核心文件清单**

基础设施层（Role/Core/）：

| 文件名 | 职责 |
| --- | --- |
| IInputProvider.cs | 输入抽象接口：MoveDirection、LookDirection、JumpPressed、ActionPressed、InteractPressed、SprintPressed |
| Blackboard.cs | 全局数据黑板：泛型 Get/Set，子控制器间共享状态 |
| IStateResponder.cs | 状态响应接口 + CharacterState 枚举（Normal/Dead/Stunned/Dialogue/Mounted/Cutscene） |
| CharacterStateCoordinator.cs | 状态协调器：Register/ChangeState 分发通知 |
| CharacterConfig.cs | ScriptableObject 配置：walkSpeed/runSpeed/jumpForce/gravity/groundedStickForce/rotationSpeed |

状态机层：

| 文件名 | 职责 |
| --- | --- |
| BaseStateMachine.cs | 状态机基类（纯 C# 类，非 MonoBehaviour）：Init/OnUpdate/ChangeState |
| FullBodyStateMachine.cs | 全身状态机：ToIdle/ToWalk/ToRun/ToJump/ToFall |
| UpperBodyStateMachine.cs | 上半身状态机 Stub |
| BaseCharacterState.cs | 状态基类：SetContext 注入 character + coordinator + stateMachine |
| IdleState.cs | 待机：有输入转 Walk，JumpPressed 转 Jump |
| WalkState.cs | 行走：CharacterController 移动 + 重力 + 转身，Sprint 转 Run |
| RunState.cs | 跑步：速度倍率，松 Sprint 转 Walk |
| JumpState.cs | 跳跃：施加初速度，速度转负后转 Fall |
| FallState.cs | 下落：重力加速，触地后转 Idle/Walk/Run |

输入层（Role/Input/）：

| 文件名 | 职责 |
| --- | --- |
| PlayerInputProvider.cs | 实现 IInputProvider + IPlayerActions，桥接 Input System |
| PlayerInputActions.inputactions | Input Actions 资产：Move/Look/Jump/PrimaryAction/Sprint/Interact |

**Unity Input System 集成**

Action 绑定：

| Action | 类型 | 绑定 |
| --- | --- | --- |
| Move | Vector2 (2D Vector composite) | W/A/S/D 键盘 |
| Look | Vector2 | Mouse/delta |
| Jump | Button | Space 键 |
| PrimaryAction | Button | 鼠标左键 |
| Sprint | Button | Left Shift 键 |
| Interact | Button | E 键 |

PlayerInputProvider 直接实现生成的 IPlayerActions 接口，通过 CallbackContext 读取输入值。跳过了 PlayerInput 组件 + SendMessage 方案，减少 Inspector 配置复杂度。

**编译错误修复**

| 错误 | 文件 | 修复方式 |
| --- | --- | --- |
| IInputProvider 找不到 | CharacterRoot / PlayerInputProvider | 添加 `using Role.Core` |
| EventBus / EventName 找不到 | CharacterStateCoordinator | 添加 `using Core` |
| OnUpdate() 隐藏基类方法 | FullBodySM / UpperBodySM | 删除冗余 OnUpdate() |

### 2026-07-03｜角色移动跑通 + 交互系统

**3.1 角色移动跑通**

BaseStateMachine 架构修复：原设计为 MonoBehaviour，但实际应该是纯 C# 类。改为由 CharacterRoot 在 Awake 中 new 创建，状态内通过 `character.fullBodySM` 访问状态机（不使用 GetComponent）。ChangeState 时自动调 SetContext 注入 character + coordinator + stateMachine。

移动调试过程：角色初始「没反应」→ 加 Debug.Log 发现 Update 在跑、状态在切换 → MoveDirection 日志显示 (0,0) 但 sqrMag=1（日志只打印 x/y，实际 z 有值）→ Position 在变但角色看不见（isGrounded=False，角色往下掉，无地面）→ 加 Plane 地面 + 写 CameraFollow 脚本 → 移动跑通。

**3.2 CameraFollow 相机跟随**

命名空间冲突修复（CS0234）：CameraFollow.cs 在 namespace Role 下，`Input.GetAxis("Mouse X")` 中的 Input 被编译器解析为 Role.Input 命名空间，而非 UnityEngine.Input 类。修复：改为 `UnityEngine.Input.GetAxis(...)` 完全限定名。

相机参数：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| distance | 5 | 相机距离角色的水平距离 |
| height | 2 | 相机高度偏移 |
| rotationSpeed | 3 | 鼠标旋转灵敏度 |
| pitch 限制 | -20°~60° | 上下角度限制 |
| SmoothDamp | 0.1 | 位置跟随平滑时间 |

**3.3 动画系统对接**

- 动画事件 PlayFootSound 修复：角色动画 clip 自带 AnimationEvent「PlayFootSound」，但角色上没有同名方法接收。在 AudioController 中添加空方法 `public void PlayFootSound()`，日后接入脚步音效。
- Root Motion 兼容：动画 clip 自带位置运动（Root Motion），与代码位移冲突。CharacterRoot 添加空 `OnAnimatorMove()` 接管 Root Motion 控制权，动画位移被丢弃，所有位移由 `CharacterController.Move()` 驱动。
- 角色朝向跟随 + 相机转向适配：CharacterRoot 新增 `RotateToward(Vector3)` 方法（Slerp 平滑插值，rotationSpeed=12）；MoveDirection 已是相机相对方向，加入 RotateToward 后角色自动跟随相机朝向。

Animator 参数：

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| Speed | Float | 0=Idle, 0.5=Walk, 1=Run，驱动 1D Blend Tree |
| IsGrounded | Bool | true=地面状态, false=空中状态 |

**3.4 重构：CharacterConfig (ScriptableObject)**

动机：速度参数直接挂在 CharacterRoot 上会增加耦合度，CharacterRoot 承担配置容器职责会越来越臃肿。

改动：

- 新增 Role/Core/CharacterConfig.cs（ScriptableObject）：walkSpeed=3.5 / runSpeed=7 / jumpForce=8 / gravity=-20 / groundedStickForce=-2 / rotationSpeed=12
- CharacterRoot 去掉所有速度字段，改为持有 config 引用，Config 属性暴露
- 各 State 去掉各自 `_gravity`/`_walkSpeed` 等私有字段，统一通过 `character.Config.xxx` 访问
- Config 为 null 时 Awake 会 LogError

**3.5 交互系统搭建**

IInteractable 接口在独立 namespace Interaction（不依赖 Role）。InteractionDetector 挂玩家子物体检测附近可交互对象。PickupItem 等世界物体实现 IInteractable，通过 `GameRoot.Instance.Inventory` 获取 IInventory 接口入背包。

新增文件：

| 文件 | 命名空间 | 职责 |
| --- | --- | --- |
| Interaction/IInteractable.cs | Interaction | 共享接口：CanInteract / GetPrompt / OnInteract |
| Interaction/PickupItem.cs | Interaction | 拾取物实现：OnInteract 调用 Inventory.AddItem，成功后 Destroy |
| Role/Interaction/InteractionDetector.cs | Role.Interaction | 挂玩家子物体，OverlapSphere 检测，选最近目标，按 E 触发 |
| View/InteractionPromptView.cs | View | 订阅 EventBus 显示/隐藏交互提示文本 |

改动文件：

- IInputProvider.cs 新增 InteractPressed（边缘触发）
- PlayerInputProvider.cs 实现交互键，通过 Input Actions 回调 OnInteract performed 触发
- PlayerInputActions.inputactions + PlayerInputActions.cs 新增 Interact 动作（E 键绑定）
- EventName.cs 新增 Interaction_TargetChanged / Interaction_Performed

**3.6 交互系统调试与修复**

- **Bug 1：Trigger 回调不触发** — 根因：Unity Trigger 回调要求至少一方有 Rigidbody，角色用 CharacterController 无 Rigidbody，子物体 SphereCollider 无法触发 OnTriggerEnter。修复：改用 `Physics.OverlapSphereNonAlloc` 主动检测，不依赖物理回调。
- **Bug 2：InteractionPromptView SetActive 自杀** — 根因：OnEnable 中 SetActive(false) 禁用了脚本自身所在物体，触发 OnDisable → 取消 EventBus 订阅 → 永远收不到事件。修复：移除 SetActive，简化为直接挂在 Text 物体上，Text.text 空串=隐藏、有值=显示。
- **Bug 3：Panel Image 挡屏** — 根因：按指导创建了 Panel + Image + 子 Text，Panel 的 Image 覆盖全屏。修复：简化方案——直接在 Text 物体上挂脚本，不要 Panel/Image/CanvasGroup。
- **Bug 4：Awake 执行顺序导致 inputProvider 为 null** — 根因：Unity 不保证 Awake 执行顺序，InteractionDetector.Awake 可能先于 CharacterRoot.Awake 执行，root.inputProvider 还未被赋值。修复：Awake 中只缓存 CharacterRoot 引用，Update 中延迟获取 inputProvider（首次非 null 后缓存）。教训：跨 MonoBehaviour 的引用不要在 Awake 中缓存，用 Start 或 Update 中延迟读取。

**3.7 性能优化**

OverlapSphere 三项优化：

| 优化项 | 说明 |
| --- | --- |
| LayerMask 过滤 | 只检测可交互层，跳过地形/墙壁等无关 Collider |
| 降频检测 | 0.1 秒一次（detectInterval），交互不需要每帧精度 |
| OverlapSphereNonAlloc | 预分配 Collider[16] 数组复用，零 GC |

其他优化：

- 目标变化时才 Emit 事件（不变化不广播）
- inputProvider 只读一次后缓存（不每帧重复读取）
- inputProvider 为空的 Warning 只打印一次（_inputMissingWarned 标志）
- 移除所有调试 Debug.Log（GC + 控制台 IO 是真正的性能隐患）

**3.8 今日完成状态**

| 模块 | 状态 |
| --- | --- |
| 角色移动（WASD/Shift/Space） | ✓ 完成 |
| 相机跟随 + 鼠标旋转 | ✓ 完成 |
| 动画 Blend Tree（Idle/Walk/Run） | ✓ 完成 |
| CharacterConfig 配置系统 | ✓ 完成 |
| 交互系统（检测+拾取+UI提示） | ✓ 完成 |
| 性能优化 | ✓ 完成 |

### 2026-08-28｜统一输入系统（消除新旧 Input 混用）

**背景 / 根因**

之前的代码新旧输入系统混用：

- `CameraFollow` 用旧 Input Manager 的 `UnityEngine.Input.GetAxis("Mouse X/Y")` 旋转相机
- `GameRoot` 用旧 Input Manager 的 `Input.GetKeyDown(I/C/Tab/Escape)` 处理 UI 快捷键
- 而 `PlayerInputProvider` 已用 Input System 的 Input Action

若工程 Active Input Handling 设为 Input System Only，旧 Input 调用会抛 `InvalidOperationException`。本次把所有输入统一到 Input System。

**改动内容**

| 文件 | 改动 |
| --- | --- |
| Role/Core/IUiInputProvider.cs | 新增 UI 输入接口（OpenBagPressed / OpenCraftPressed / TogglePanelPressed / CloseAllPressed） |
| Role/Core/IInputProvider.cs | 新增 `LookDelta`（Vector2）：本帧鼠标/右摇杆旋转增量 |
| Role/Input/PlayerInputActions.inputactions | 新增 4 个 action：OpenBag(I) / OpenCraft(C) / TogglePanel(Tab) / CloseAll(Esc) |
| Role/Input/PlayerInputActions.cs | 同步生成代码（字段 / 属性 / 回调 / 接口方法） |
| Role/Input/PlayerInputProvider.cs | 实现 `LookDelta` + `IUiInputProvider`，新增 4 个 UI 输入缓存 + 回调 + LateUpdate 清理 |
| Role/CameraFollow.cs | 相机旋转改走 `IInputProvider.LookDelta`，移除 `UnityEngine.Input.GetAxis` |
| GameRoot.cs | UI 快捷键改走 `IUiInputProvider`，移除 `Input.GetKeyDown`，延迟获取并缓存 PlayerInputProvider |

**设计决策**

- UI 输入与角色移动输入分离成两个接口（`IInputProvider` / `IUiInputProvider`）：`IInputProvider` 是为 AI 预留的「角色玩法输入」契约，AI 不会开背包；把 UI 快捷键塞进去会污染接口语义。`PlayerInputProvider` 同时实现两个接口。
- `LookDelta`（相机旋转）与 `LookDirection`（瞄准方向）语义不同，不合并。

**回家对接情况**
- ✅ 跑通的：
- ⚠️ 要改的：
- 🐛 新踩的坑：

**下一步**
- 继续修 P0：GameRoot/View 判空、SqliteManager 源库校验、CraftController 事务绑定。

**Unity 手动操作清单（回家必做）**
- 无需拖拽赋值：新增引用均通过 `GetComponentInChildren` / `FindObjectOfType` 自动获取。
- `PlayerInputProvider` 必须挂在角色 GameObject 或其子物体上（CameraFollow 通过 `target.GetComponentInChildren<IInputProvider>()` 查找，GameRoot 通过 `FindObjectOfType<PlayerInputProvider>()` 查找）。
- 若在 Unity 里改动 `PlayerInputActions.inputactions` 并勾选 Generate C# Class，会覆盖手写的 `PlayerInputActions.cs`，需重新同步（或直接使用仓库内已同步的两份文件）。
- 确认 Active Input Handling 为 Input System 或 Both（本次已彻底移除旧 Input 调用）。

**待验证项**
- `GetComponentInChildren<IInputProvider>()` 接口查询：项目已沿用此写法（CharacterRoot 同款），Unity 2019.1+ 理论支持，回家首次运行确认能拿到。

### 2026-08-28（续）｜补齐 P0：判空 / 源库校验 / 事务绑定

**本次范围**

按评估的 P0 顺序，修复三类「真机会直接报错」的缺陷。

**1. SqliteManager 源库校验 + 事务统一管理**

- `Init()` 改为返回 `bool`：源库不存在、`File.Copy` 失败、`Open` 失败时 `LogError` 并返回 false，不再裸抛异常。
- 新增「当前事务」统一管理：`BeginTransaction()` 改为 void（内部持有 `_currentTransaction`），新增 `CommitTransaction()` / `RollbackTransaction()`。
- `ExecuteNonQuery` / `ExecuteQuery` 创建命令时自动绑定 `_currentTransaction`——**本次最关键修复**：此前 `CraftController` 拿到的 transaction 对象没有传给 DAO 的 SqliteCommand，`BagDao.UpdateSlot` 实际是自动提交的，`Rollback` 根本回滚不了任何东西。
- `OnApplicationQuit` 补事务清理。

**2. CraftController 事务绑定 + 空间预检**

- `DoCraft` 改用 `SqliteManager` 统一事务 API。
- 新增 `IInventory.CanAddItem(itemId, count)` + `BagController` 实现：制作前预检背包空间。原因：背包是「内存优先 + DB 持久化」架构，`AddItem` 中途失败会让内存与 DB 不一致；预检保证事务内 `AddItem` 必成功，从根上避免「回滚了 DB 但内存已变」。

**3. GameRoot / View 判空**

- `GameRoot.InitAllModule`：判空 `SqliteManager.Instance` + 检查 `Init()` 返回值；`BagView` / `CraftView` 逐个判空降级；`UIController.Init()` 移到面板注册之后（原先在注册前调用，`OpenBagPanel` 实际无效果）。
- 新增 `_initialized` 标志，初始化失败时 `Update` 直接 return，避免空引用连锁。
- `BagView`：`SetController` / `InitSlots`（防重复生成）/ `RefreshUI`（controller 判空 + 防越界 + 空格子跳过 DB 查询 + 容量文本移出循环）。
- `CraftView`：`SetController`（材料引用一次性校验）/ `RegisterButtonEvents` / `InitRecipeList` / `RefreshDetail`（TryGetValue 防 KeyNotFound + Inventory 判空）/ `RefreshMaterialList` / `OnCraftButtonClick`。
- `BagSlotItem` / `CraftMaterialItem` / `CraftRecipeItem`：序列化引用判空，同类 Warning 只打印一次。

**改动文件清单**

| 文件 | 改动 |
| --- | --- |
| Core/SqliteManager.cs | Init 返回 bool + 源库校验；事务统一管理；命令自动绑定事务 |
| Core/IInventory.cs | 新增 CanAddItem |
| Controller/BagController.cs | 实现 CanAddItem |
| Controller/CraftController.cs | DoCraft 事务绑定 + 空间预检 |
| GameRoot.cs | 判空 + 初始化顺序 + _initialized 标志 |
| View/BagView.cs | SetController / InitSlots / RefreshUI 判空防越界 |
| View/CraftView.cs | 全链路判空 |
| View/BagSlotItem.cs / CraftMaterialItem.cs / CraftRecipeItem.cs | 序列化引用判空 |

**回家对接情况**
- ✅ 跑通的：
- ⚠️ 要改的：
- 🐛 新踩的坑：

**已知限制 / 下一步**

- 「制作 ×N」当前是 N 个独立事务（`OnCraftButtonClick` 循环调 `DoCraft`），非单事务原子批量；若中途失败会部分成功。后续可把循环下沉到 `DoCraft` 内部用单个事务包裹。
- 事务内 `EventBus.Emit(Bag_Changed)` 会在提交前触发 View 刷新（读内存 + 配置表），正常路径无碍；若 Commit 前异常回滚，内存与 DB 会出现不一致——这是「内存优先」架构的固有边界，正常制作路径已通过空间预检规避，极端 DB 异常场景留待后续重构内存回滚。

**Unity 手动操作清单（回家必做）**
- 无需拖拽赋值：本次无新增序列化引用。
- 确认场景中存在 `SqliteManager`（已带 `[DefaultExecutionOrder(-100)]`，保证先于 GameRoot Awake）。
- 确认 `StreamingAssets/game.db` 存在（缺失会在控制台给出明确 LogError）。
- 本次无新增 Input Action / Layer / Tag。

**待验证项**
- `SqliteCommand.Transaction = _currentTransaction`：Mono.Data.Sqlite 的 SqliteCommand 继承自 DbCommand，Transaction 属性类型为 SqliteTransaction，赋值合法。回家跑一次制作确认扣材料与加成品在 DB 层原子生效。

### 2026-08-30｜修复合并丢失：输入系统统一改动找回

**现象**

日志 2026-08-28 声称「已统一输入系统」，但当前工程里 `IUiInputProvider` / `LookDelta` 完全搜不到，`GameRoot` 仍是 `Input.GetKeyDown`、`CameraFollow` 仍是 `Input.GetAxis`——**日志与代码不一致**。

**根因（git 证据链）**

- `f66e0a7`（HEAD）名为「合并公司轻开发与本地Role系统」，但 reflog 显示它是 `commit (initial)`，**是一个无父根提交，不是真正的 merge**。
- 它实际做的是目录重构：`Assets/Assets/Scripts/**` → `Assets/Scripts/**`，重构过程中把输入系统统一的改动丢了。
- 旧版本 `ada68fe`（远端 `mirror/legacy` 分支）里这些代码**完好存在**。
- 比对：`git diff -M20% ada68fe HEAD -- 'Assets/**/*.cs'` → 仅 **7 个文件有内容差异（8 增 / 313 删）**，其余 42 个脚本是纯路径重命名（0 变化），说明丢失范围可控、没有别的暗坑。

| 文件 | 丢失内容 |
| --- | --- |
| Role/Core/IUiInputProvider.cs | 整文件丢失（21 行） |
| Role/Core/IInputProvider.cs | `LookDelta` 属性 |
| Role/Input/PlayerInputActions.inputactions | OpenBag / OpenCraft / TogglePanel / CloseAll 四个 action |
| Role/Input/PlayerInputActions.cs | 生成代码同步（回调注册 + 接口声明） |
| Role/Input/PlayerInputProvider.cs | 实现 `IUiInputProvider` + `LookDelta` + 4 个 UI 回调 + LateUpdate 清理 |
| Role/CameraFollow.cs | 相机旋转改走 `LookDelta`，延迟获取输入（Awake 顺序坑） |
| GameRoot.cs | UI 快捷键改走 `IUiInputProvider`，延迟获取 + Warning 只打一次 |

**修复方式**

从 `ada68fe` 取回原版（旧路径）后，逐份用 `git diff --no-index` 与新路径版本核对，确认 6 份均为「旧版 ⊇ 新版」的纯删除差异，**不存在需要手工合并的冲突**，因此整文件字节级还原，未做任何手工改写。
`IUiInputProvider.cs` 在旧仓库里没有 `.meta`（当年是无 Unity 环境手写、从未被导入过），由 Unity 下次导入自动生成。

**验证结果**

本机 Unity 无头编译：`-batchmode -nographics -quit -logFile <tmp>` → **0 error / 0 warning**，`Exiting batchmode successfully now!`；
`IUiInputProvider.cs` 已正常导入并生成 `.meta`（guid `315c794478c7d5b448dffd13593d6391`）。

> 首次跑通「不开编辑器 → 命令行编译 → 读日志」闭环，耗时约 90 秒，日志落 `%TEMP%` 避免污染仓库。**后续盲写代码都可以用它自检**，不必等到回家开 Unity 才发现编译错误。

**遗留问题（同日已处理，见下方补充）**

- `Assets/Scripts/DebugTool.cs` 仍有 5 处 `Input.GetKeyDown(F1~F5)`。该文件在 `ada68fe` 与 HEAD 中内容一致，**不是这次丢的，是当初就漏改了**。若 Active Input Handling = Input System Only，运行到它会抛 `InvalidOperationException`。

**回家对接情况**
- ✅ 跑通的：本机 Unity 无头编译闭环（`-batchmode -nographics -quit`）
- ⚠️ 要改的：
- 🐛 新踩的坑：

**下一步**
- 回家后跑一次运行模式：验证 `GameRoot` / `CameraFollow` 的延迟获取逻辑、`OpenBag/Craft/Tab/Esc` 快捷键、相机 `LookDelta` 旋转。
- 将 Active Input Handling 切换为 Input System Only（`DebugTool` 已移除，无旧 Input 残留）。

**Unity 手动操作清单（回家必做）**
- 无需拖拽赋值：`GameRoot` 用 `FindObjectOfType<PlayerInputProvider>()` 延迟获取，`CameraFollow` 用 `target.GetComponentInChildren<IInputProvider>()`。
- `PlayerInputProvider` 必须挂在角色 GameObject 或其子物体上，否则 UI 快捷键与相机旋转不可用（控制台各打印一次 Warning）。
- 确认 Active Input Handling 为 Input System 或 Both；`DebugTool` 已移除，现在可以直接设为 Input System Only。

### 2026-08-30（补充）｜DebugTool 改造为 Editor 菜单项

**动机**

`DebugTool.cs` 是运行时测试脚本，用 F1~F5（旧 Input Manager）加物品/清背包，是输入系统统一后**唯一的旧 Input 残留**，Input System Only 下运行即抛 `InvalidOperationException`；且调试入口不该进包体。

**改动**

- 删除 `Assets/Scripts/DebugTool.cs`（+ `.meta`）。删除前已核对：GUID `b5bd7b1e257b4934e8b774b5e5d138df` 未被任何场景/预制体引用（该脚本从未挂到场景），不会产生 Missing Script。
- 新增 `Assets/Scripts/Editor/BagDebugMenu.cs`：`Tools/背包调试/` 菜单，5 个功能对应原 F1~F5：
  - 添加 生铁 x5 / 木头 x5 / 胶带 x5（priority 11~13，同组）
  - 每种材料 x10（1001~1005）
  - 清空背包
- 全部菜单项带 `[MenuItem]` validate 方法，非运行模式自动置灰——背包是运行时数据（`GameRoot` Awake 初始化），编辑模式点击没意义。

**设计要点**

- Editor 文件夹 → 仅编辑器编译，不参与打包，无任何输入系统依赖。
- 判空链完整：`GameRoot.Instance` → `BagController` → `GetAllSlots()`，每层缺失都打明确 Warning。
- 遍历安全说明：`RemoveItem` 只改写格子内的 `ItemId/ItemCount`，不增删列表元素，foreach 不炸。
- Editor-only 代码允许 `Debug.Log`（不参与运行时，无每帧 GC/IO 开销），与「正式代码禁 Debug.Log」的规则不冲突。

**验证结果**

本机 Unity 无头编译第二次跑通：**0 error / 0 warning**，`Exiting batchmode successfully now!`；`BagDebugMenu.cs` 正常导入生成 `.meta`（guid `eec147900bfa72a4fa07642158aeba19`）。

> 现在全仓库旧 `Input.GetKeyDown` / `Input.GetAxis` 调用清零，输入系统统一闭环。

### 2026-08-30（补充 2）｜P0：AI 自检工具方案（记录，暂未实施）

**背景**

第一次会话评估「接入 MCP 做前端自动化」的结论：现阶段不建议接第三方 Unity MCP（Unity 官方 MCP 需 Unity 6，第三方仅支持 Unity 6 编辑器 API，本项目 2022.3 不可用）。改用**近乎零成本的 Editor 静态类替代方案**拿到约 80% 核心收益，即 P0。

**P0 方案**

写 Editor 静态类，让 Unity 无头模式自动做两类自检，结果写文件，AI 读文件形成「盲写代码 → 自检 → 修复 → 再自检」闭环：

1. **CompileCheck（无头编译）**：命令行 `-batchmode -nographics -quit` 编译工程，导出编译错误。
   - ✅ 已完成：本次会话已 3 次手动跑通该命令（验证 `IUiInputProvider`、`BagDebugMenu`），日志落 `%TEMP%`，约 90 秒，开发日志 386 行有记录。
   - ❌ 未做：沉淀成可复用工具（Editor 静态类/`Tools/自检/` 菜单项），参数化 + 退出码。
2. **SceneAudit（场景自检）**：加载场景后程序化检查：
   - 场景中是否存在 `SqliteManager`
   - 角色预制体组件齐全（`PlayerInputProvider` 等）
   - `BagView` / `CraftView` 的 `[SerializeField]` 引用非空
   - 结果写文件（如 `%TEMP%/unity_scene_audit.log`），PASS/FAIL 退出码区分
   - ❌ 未做：完全未开始。

**为什么值得做**

- 现状「编译通过 ≠ 能跑」：场景/配置类错误（管理器不在场景、组件没挂、引用未赋值）只能回家开 Unity 人肉看；SceneAudit 让 AI 在纯代码仓库里也能查出这类问题。
- 把「回家才发现」提前到「写完立刻发现」，减少验证往返。
- 属于 CI/CD 前身的真实工程实践，可写进毕设。

**决策边界**

- 只覆盖客观检查（编译、引用、配置）；玩法/手感等主观问题仍需人肉跑。
- 若开发流是「总在 Unity 编辑器旁」，此工具锦上添花，可推迟。

**状态：已记录，未实施，待确认后开工。**

### 2026-08-30（补充 3）｜P0 自检工具框架落地（可运行）

按「补充 2」的方案落地了可运行框架，交付 4 个文件：

| 文件 | 内容 |
| --- | --- |
| `Assets/Scripts/Editor/SceneAudit.cs` | 场景自检框架：检查器注册机制（`RegisterChecker`）+ 内置 6 项检查（GameRoot / SqliteManager / PlayerInputProvider / BagView 引用 / CraftView 引用 / CameraFollow 引用）+ 菜单 `Tools/自检/场景自检` + 命令行入口 `-executeMethod EditorTools.SceneAudit.RunFromCommandLine` |
| `Assets/Scripts/Editor/CompileCheck.cs` | Unity.exe 路径探测（UNITY_PATH > 项目版本匹配常见目录 > Hub 扫描）+ 命令生成 + 菜单复制命令/探测路径 |
| `Tools/run_check.ps1` | 一键自检：编译检查 + 场景自检 + 汇总，退出码 0/1；纯 ASCII 规避 PowerShell 5.1 编码坑 |
| `Assets/Scripts/Editor/README-自检工具.md` | 给「公司开发时的另一个模型」的完整上下文：用法、结果判读、扩展指南、项目约定、待完善清单 |

**设计要点**

- 结果闭环：自检结果写 `%TEMP%/unity_scene_audit.log`（末尾 `SCENE_AUDIT_RESULT: PASS/FAIL`），编译错误写 `%TEMP%/unity_compile_check.log`（`error CS` 行），AI 读文件即可定位，不需要开编辑器。
- 场景范围：`SCENE_AUDIT_SCENE` 环境变量 > Build Settings 启用的场景 > `Assets/Scenes` 兜底。
- 检查器逐项 try/catch：单个检查器异常不会中断整个自检，记 FAIL 继续。
- 用 `SerializedObject.FindProperty` 读私有 `[SerializeField]` 引用，CraftView 区分必填/次要引用（次要缺失不判 FAIL 只提示）。
- 公司机器接力：README 第七节列了待完善清单（验证脚本、角色预制体检查、图标齐全检查、接入日常流程）。

**待验证**

- 本机无头编译验证 Editor 新代码可编译 + 实际跑一次场景自检（见下文验证结果）。

### 2026-08-30（补充 4）｜事故记录：无头自检误杀编辑器进程 + 互斥修复

**事故**

- 用户开着 Unity 编辑器时，我跑无头编译 + 场景自检。无头实例崩溃后，我在清理时执行
  `Get-Process Unity | Stop-Process -Id $_.Id` 无差别杀进程，把用户开着的编辑器也结束了。
- 根因 1（操作失误）：`Get-Process Unity` 返回所有 Unity 进程（编辑器 + 无头实例），
  `-Id` 传数组会全杀。应只记录自己 `Start-Process` 返回的精确 PID 再定向清理。
- 根因 2（流程缺陷）：无头编译（`-batchmode -projectPath`）与「打开着同一项目的编辑器」
  同时运行是 Unity 官方不支持的双实例用法（项目锁 + 并发写 `Library` 缓存），跑之前
  必须检测编辑器占用，不能直接跑。
- 根因 3（机器特性，非互斥所致）：崩溃报告显示无头进程自身在 Licensing 启动期偶发崩溃
  （`Code 10 while verifying Licensing Client signature` / `Access token is unavailable`），
  重试第二次成功——与编辑器是否打开无关，`run_check.ps1` 已内置自动重试。

**修复**

- `run_check.ps1` 启动前用 `Get-CimInstance Win32_Process` 按命令行匹配项目路径，
  检测到编辑器已打开本项目则中止（退出码 3，提示先关编辑器）；`-Force` 可跳过。
- 脚本内超时清理只杀自己启动的 `$p.Id`（本来就如此），后续手工操作一律禁止无差别杀 Unity。
- README 加「互斥」醒目警告。

**以后的固定流程**（写给自己）：跑任何无头编译/自检前，先 `Get-CimInstance Win32_Process
-Filter "Name='Unity.exe'"` 确认没有实例打开本项目；有则先让用户保存关闭，绝不强跑、
绝不无差别杀进程。

### 2026-08-31｜补无 exe 自检层（修正 P0 设计疏漏）

**背景 / 根因**

8-30 的 P0 自检工具声称服务「无 Unity 环境」，但实现手段是 `Unity.exe -batchmode`，依赖本机装了 Unity.exe。到了真正无 Unity 的机器（当前公司环境），三个组件全部无法运行——目标与手段自相矛盾。根因：策划时身处「有 Unity.exe 的家」，把「无 Unity 编辑器（GUI）」误解为「无 Unity.exe」。

**改动**

- 新增 `Tools/static_check.ps1`：纯 PowerShell 文本解析，零依赖，不需要 Unity.exe。检查项：
  - FAIL 级：非 Editor 目录禁用 `Debug.Log`、`.cs/.inputactions` 缺 `.meta`、字符串硬编码查找（SendMessage / GetComponent("...") / GameObject.Find / FindObjectOfType）
  - 场景/预制体文本检查：解析 `.unity/.prefab` 的 `m_Script: {fileID: 0}` 找 missing script（当前仓库无场景文件，SKIP 待回家生效）
  - INFO 级：单行超 120 字符、私有字段 `_` 前缀启发式
- 改造 `Tools/run_check.ps1`：新增 Step 0 静态检查（永远跑）；Unity.exe 找不到时优雅降级跳过编译/场景，不再 throw 中止。
- `static_check.ps1` 字段命名检查加「Unity 生成代码白名单」：识别文件头 `// <auto-generated>`（Input System 代码生成器等），跳过这类机器生成文件——其 `m_` 前缀是生成惯例，不应按手写代码 `_` 前缀规则误报。

**验证结果**

- 本机（无 Unity）跑通 `static_check.ps1`，初次抓到 `EventBus.cs` 7 处 `Debug.Log`。
- 复核确认这 7 处是刻意设计：5 处受 `DebugMode` 开关保护（默认 false）、3 处在 `DumpSubscriptions()` 调试方法内，非「忘删的调试代码」。
- 据此给检查器加了「开关保护 / 调试方法」识别，重跑后 EventBus 不再误报，结果 `STATIC_AUDIT_RESULT: PASS`。
- `run_check.ps1` 降级验证通过：Static PASS + Compile SKIPPED + Scene SKIPPED，退出码 0。
- 字段命名白名单验证通过：`PlayerInputActions.cs`（auto-generated）被正确跳过，其余手写代码字段命名 OK，结果 `STATIC_AUDIT_RESULT: PASS`。

**回家对接情况**
- ✅ 跑通的：
  - `run_check.ps1` 三查齐全全部通过：`AUTO CHECK: ALL PASS`。Static PASS（pass=4 fail=0 **skip=0**）+ Compile PASS（**warnings: 0**）+ Scene PASS（pass=6 fail=0）。
  - 场景/预制体 missing-script 文本检查在家生效：公司那台因仓库无场景文件 `skip=1`，在家 `skip=0`，该项转为实际检查且 PASS。
  - 字段命名白名单生效：`PlayerInputActions.cs`（`// <auto-generated>`）被正确跳过，未误报。
  - 场景对象齐全：GameRoot / SqliteManager / PlayerInputProvider 均存在；BagView、CraftView 引用完整；CameraFollow 存在且 target 已赋值。
- ⚠️ 要改的：
  - 无。
- 🐛 新踩的坑：
  - Unity.exe **不在** Program Files / Unity Hub 默认路径，而在 `D:\unity\2022.3.57f1c2\Editor\Unity.exe`（与 `ProjectSettings/ProjectVersion.txt` 要求的版本一致）。探测环境时若只查 `C:\Program Files\Unity\Hub\Editor` 会误判为"本机无 Unity"，从而误以为编译/场景检查跑不了——实际 `Get-UnityExe` 的 `D:\unity\<version>\Editor\Unity.exe` 候选路径能正常命中。

**下一步**
- 回家跑 `run_check.ps1`（有 Unity 时三查齐全），验证场景/预制体文本检查。

**Unity 手动操作清单（回家必做）**
- 无新增序列化引用。
- 回家跑 `powershell -ExecutionPolicy Bypass -File Tools\run_check.ps1`。

---

### 2026-08-31｜修复 CraftController 事务形同虚设 + 背包双写一致性

**背景 / 根因**

制作流程 `CraftController.DoCraft` 虽包裹了事务，但背包是「内存为主（`_slotList`）+ 数据库镜像」的双写架构：`AddItem`/`RemoveItem` 先改内存、再写库。事务回滚只回数据库、不回内存，一旦失败内存与数据库永久分叉——事务对上层系统形同虚设。

**改动**

- `SqliteManager` 新增作用域事务 `RunInTransaction(Action)`：正常提交、异常回滚、finally 释放清空，杜绝「忘提交/忘回滚」与「重复开启被忽略」的僵尸事务；移除手动的 `BeginTransaction/Commit/Rollback`。
- `BagController.AddItem` 改为「副本预演 → 事务写库 → 原地更新内存」，消除空间不足时的「改一半内存/库」部分写入问题。
- `BagController.TryCraft` 改用 `RunInTransaction` + `ApplySnapshotToLive` 原地更新（不再替换 `_slotList` 引用，避免外部持有旧引用失效）。
- `RemoveItem` 保持原实现：已有 `total` 预检兜底，事务包裹本意同为兜底，属「决策保留」而非技术限制。若未来要拉齐，可用 `ApplySnapshotToLive`（原地写回字段、不替换列表引用）复用同一模式，`foreach` 遍历时修改元素字段合法，不会破坏 `ClearBag`。

**验证结果**

- `static_check.ps1` 跑通：`STATIC_AUDIT_RESULT: PASS`（pass=3 fail=0 skip=1）。
- 本机无 Unity，无法编译验证 C#；待回家用 `run_check.ps1` 跑编译检查确认无语法错误。

**回家对接情况**
- ✅ 跑通的：
  - 编译验证通过：`run_check.ps1` Compile PASS，**warnings: 0**——白天改动的 300+ 行 C#（BagController +205 / CraftController -47 / SqliteManager +54 / IInventory +5）无语法、引用错误。
  - 场景检查 PASS（pass=6 fail=0）。
  - 制作流程三种路径代码审查，原子性均正确（「副本预演」使部分写入从机制上不可能发生）：
    - **材料足够**：副本扣料 → 加成品 → `RunInTransaction` 写库 → `ApplySnapshotToLive` 原地更新 → `Emit(Bag_Changed)` → true。
    - **材料不足**：`ApplyRemoveTo` 在副本上失败 → `failReason="材料不足"` → 副本丢弃，原 `_slotList` 与数据库零改动。
    - **背包满**：副本已扣料但 `ApplyAddTo` 失败（`remain > 0` → `reason="背包空间不足"`）→ 副本丢弃，原状态零改动。**这正是旧代码病灶所在**——旧版在 `AddItem` 失败时材料已扣进内存和库。
- ⚠️ 要改的：
  - 无功能改动。`RemoveItem` 经评估**保留原实现**（决策已确认）：`total` 预检即为兜底，事务包裹的本意也是兜底，不强行拉齐三个写操作。
  - 附一条认知修正（备忘，不改动代码）：上文"不能改成副本替换"的理由表述不准确——`ApplySnapshotToLive` 是**原地写回**（`_slotList[i].ItemId = snapshot[i].ItemId`，不替换列表引用），而 `foreach` 遍历 List 时修改元素属性是合法的（禁止的是增删元素/替换集合引用），因此 `BagDebugMenu.ClearBag` 的原地语义其实不会被破坏。若未来要拉齐 Add/Remove/Craft 的严谨性，可直接复用该模式，改造成本很低。
- 🐛 新踩的坑：
  - 无。

**下一步**
- 回家跑 `run_check.ps1` 验证编译 + 场景检查；重点回归测试制作流程（材料足够/不足/背包满三种情况）。

---

### 2026-09-01｜战斗系统骨架：武器三槽切换 + 加成道具

**背景**

战斗系统正式启动，两个子任务：①独立武器栏（求生之路式三槽）+ 近战/枪械行为分离；②背包/制造里旧「武器」（阿瑞斯病毒风格，纯堆叠物品无功能）改造为「加成道具」（使用生效、永久加攻击力/近战攻速）。

**改动（代码）**

新增 `Combat/` 模块（9 个文件，纯数据与行为分离）：

| 文件 | 职责 |
| --- | --- |
| WeaponType.cs | enum：Melee / Ranged |
| WeaponSlot.cs | enum：Primary=1 / Secondary=2 / Melee=3（槽位制） |
| WeaponConfig.cs | ScriptableObject：伤害/范围/模型/动画参数 |
| IDamageable.cs | 可受伤接口（敌人实现即可被打） |
| IWeaponBehavior.cs | 武器行为策略接口 |
| MeleeWeaponBehavior.cs | 近战：OverlapSphere 范围判定 |
| RangedWeaponBehavior.cs | 枪械：Raycast 射线判定 |
| BonusItemConfig.cs | 加成道具配置（ScriptableObject）：itemId + 攻击/攻速加成% |
| CombatKeys.cs | Blackboard 键名常量 |

改造 `EquipmentController`（原 Stub → 切枪中枢）：
- 固定三槽 `WeaponConfig[3]`，键位 1/2/3 直达 `WeaponSlot`，空槽按键忽略。
- `Pickup`：落入归属槽（同槽覆盖）+ 自动 `SwitchTo`（剧情「获得枪械自动切一次」）。
- 切换过渡：`SWITCH_DURATION=0.3f` 代码计时禁攻，不依赖动画时长；期间 `CanAttack=false`。
- `CompleteSwitch`：换槽 → 换行为策略 → 更新 `Blackboard["Weapon_CurrentType"]` → `Emit(Weapon_Equipped)`。
- `OnWeaponEquipped` 表现层钩子（挂模型/动画/上半身状态机 ToAim/ToIdle）留 TODO 待回家对齐 Animator。

新增 `BonusController`（加成道具使用逻辑）：
- `Init(IInventory)`：从 `Resources/BonusItems/` 自动加载所有 `BonusItemConfig`（零拖拽）。
- `UseItem(itemId)`：查配置 → 扣 1 个道具（`RemoveItem`）→ 累加 `Blackboard[Combat_AttackMultiplier]` / `[Combat_MeleeAttackSpeedMultiplier]` → `Emit(Bonus_Used)`。
- `IsBonusItem(itemId)`：供 UI 判断是否显示「使用」按钮。

武器行为接入攻击加成：`Attack(Transform, WeaponConfig, float attackMultiplier)`，伤害 `weapon.damage × attackMultiplier`；`EquipmentController.Attack()` 从 Blackboard 读 `Combat_AttackMultiplier` 传入。

**关键设计决策**

- **槽位制而非列表循环制**：求生之路式固定三槽，键位直达，空槽忽略，无下标循环。
- **策略模式分离近战/枪械**：命中判定方式根本不同（范围 vs 射线），`IWeaponBehavior` 各实现一套；`EquipmentController` 只认抽象，不关心具体武器——资源不确定性隔离在表现层。
- **Combat → Role 单向依赖**：`IWeaponBehavior.Attack` 只依赖 `Transform + WeaponConfig`，不反向引用 Role；加成值经参数传入，保持单向。
- **加成值放 Blackboard 而非新建 CombatBonus 类**：贴合「共享数据放 Blackboard」约定，且避免 `BonusController` 拿 `CharacterRoot` 引用（GameRoot 初始化时序坑，Blackboard 静态无此问题）。
- **攻击力加成已接通，近战攻速加成仅存字段**：攻速作用在攻击冷却，攻击状态未做，暂存待接。

**验证结果**

- `static_check.ps1`：pass=2 fail=1 skip=1。唯一 FAIL 为 10 个新 `.cs` 缺 `.meta`（无 Unity 环境预期，回家导入自动生成）；禁 `Debug.Log`、无字符串硬编码查找、字段命名规范均 PASS。
- 本机无 Unity，无法编译验证；代码引用（`Physics.OverlapSphere` / `Physics.Raycast` / `GetComponentInParent<IDamageable>` 等）已逐文件人工核对。

**回家对接情况**
- ✅ 跑通的：
  - `run_check.ps1` **ALL PASS**：Static 4/4、Compile **warnings=0**、Scene 6/6（含当晚 BagSlotItem/BagView 改动编译通过，无回归）。
  - 背包「使用」按钮链路代码层闭环：`SelectSlot` → `RefreshUseButton`（`IsBonusItem` 控制显隐）→ `OnUseButtonClick` → `BonusController.UseItem`（扣 1 → 改 Blackboard → 广播 `Bonus_Used`）→ `RemoveItem` 广播 `Bag_Changed` → `RefreshUI` → 格子变空自动取消选中 → 按钮隐藏。
  - 选中高亮仿 `CraftRecipeItem`：`BagSlotItem.selected` 独立 Image + `SetActive` 切换；`itemCell.prefab` 内已有现成 `ItemSelect` 节点（与 CraftSelect 同款 sprite）可直接拖配。
- ⚠️ 要改的：
  - 无代码改动。需用户在 Unity 编辑器内手动配：① `itemCell.prefab` 的 `ItemSelect` 拖到 `BagSlotItem.selected`；② 场景 `BagView` 的 `useBtn` 拖到「使用」按钮。
  - （可选）订阅 `Bonus_Used` 弹提示，未做。
  - 另两条非今晚任务：`OnWeaponEquipped` 表现层、攻速加成待攻击冷却落地。
- 🐛 新踩的坑：
  - 无。

**git 提交记录（本次回家）**
- `feat: Combat 模块 meta 入库 + Editor 工具（DatabaseTool/CombatDebugMenu）`——11 个 `.meta`（Combat.meta + 9 个 Combat/.cs.meta + BonusController.cs.meta）必入库，否则公司端导入生成随机 GUID → missing script；Editor 工具两个。
- `feat: 背包选中高亮 + 使用按钮`——BagSlotItem / BagView / 本日志。

**下一步**
- 回家跑 `run_check.ps1` 验证编译；重点回归制作/背包流程无回归。
- 实现 `EquipmentController.OnWeaponEquipped` 表现层（挂模型 + 上半身 ToAim/ToIdle + 动画）。
- 攻击状态（攻击冷却）落地后，接入 `Combat_MeleeAttackSpeedMultiplier`。

**Unity 手动操作清单（回家必做）**
- 创建武器资产：`Create > Combat > Weapon Config`（锯子 Melee/Melee槽、枪 Ranged/Primary槽），填伤害/范围。
- 把 `EquipmentController` 的 `rightHandAttachPoint` 拖到右手挂点。
- 创建加成道具资产：`Create > Combat > Bonus Item Config`，填 itemId + 加成%，放入 `Assets/Resources/BonusItems/`。
- 数据库 `ItemConfig` 表加加成道具条目（id 与 BonusItemConfig.itemId 对应），type 顺手改 `bonus`。
- 初始装备：找地方调 `Pickup(锯子Config)`（或先空手手动调）。
- BagView 加「使用」按钮：`IsBonusItem(itemId)` 判断显示，点击 `UseItem(itemId)`。
- 可选：订阅 `Bonus_Used` 弹提示。

---

### 2026-09-02｜战斗系统代码层推进计划

今天按依赖顺序逐项推进，每项完成并静态检查后再进入下一项：

1. **修复 `EquipmentController` 两个问题**
   - 消除 `CharacterRoot.Awake` 动态创建 `CharacterStateCoordinator` 带来的初始化时序风险，避免 `_coordinator == null` 导致永久不能攻击。
   - 修复同槽拾取新武器时提前返回的问题，确保行为策略、表现层钩子及 `Weapon_Equipped` 事件正常更新。
2. **接入武器切换和攻击输入**
   - 1/2/3 对应 Primary / Secondary / Melee，空槽忽略。
   - 攻击输入接入 `EquipmentController.Attack()`，明确按下沿与持续按住语义，避免每帧无节制攻击。
3. **实现攻击冷却及攻速倍率**
   - 为武器补充可配置攻击间隔或攻速数据。
   - 近战冷却读取 `Combat_MeleeAttackSpeedMultiplier`，让加成道具真正影响玩法。
4. **搭建 UpperBody 状态机接口**
   - 先完成与动画资源解耦的状态/转换接口及 Animator 扩展点。
   - 具体 Avatar Mask、动画片段和武器模型挂载留到有 Unity 与本地资源的环境验证。

**第 1 步完成：`EquipmentController` 稳定性修复**

- 初始化时序：移除 `EquipmentController.Awake` 自行查找依赖，改由 `CharacterRoot.Awake` 在协调器创建完成后调用 `equipmentCtrl.Init(this, coordinator)` 显式注入，消除 Unity 脚本 `Awake` 顺序不确定导致永久不能攻击的风险。
- 同槽替换：新增独立 `_currentWeapon` 指针，将「槽位里持有的武器」与「当前实际装备的武器」分离；同槽覆盖时旧武器一直保留到切换完成，届时正确重建行为策略并广播 `(oldWeapon, newWeapon)`。
- 切换健壮性：重复请求同一待切目标不重置计时；切回当前武器会取消待完成切换；非法 `WeaponSlot` 值通过统一索引校验安全忽略。
- 验证：`static_check.ps1` PASS（pass=3 fail=0 skip=1），代码诊断 0，`git diff --check` 通过。公司端无 Unity，运行时场景验证待回家完成。

**第 2 步完成：武器切换与攻击输入**

- Input Actions：将含义模糊的 `PrimaryAction` 改名为 `Attack`；新增 `SelectPrimary` / `SelectSecondary` / `SelectMelee`，分别绑定主键盘数字 `1/2/3`。源 `.inputactions` 与自动生成包装器已同步，回家由 Unity 重新导入复核生成结果。
- 输入契约：`IInputProvider` 新增 `AttackPressedThisFrame`、`AttackHeld` 及三个槽位边缘触发属性；玩家与未来 AI 使用同一套语义，不把战斗输入塞入 `IUiInputProvider`。
- Provider：攻击明确拆分「按下沿」和「持续按住」；数字键只产生单帧命令；`LateUpdate` 清边缘标志，`OnDisable` 清全部缓存，避免状态跨禁用残留。
- 输入路由：`CharacterRoot.HandleCombatInput` 将 `1/2/3` 映射到固定槽位，再用攻击按下沿调用一次 `EquipmentController.Attack()`；同帧切换优先于攻击，因此攻击会被切换状态安全拦截。`AttackHeld` 留给下一步冷却/自动武器使用，本步不逐帧攻击。
- 验证：源 Input Action JSON 可解析；源资产与生成包装器内嵌 JSON 一致；30 个 Action/Binding ID 无重复；旧 `PrimaryAction` / `ActionPressed` 引用清零；静态检查 PASS、代码诊断 0、`git diff --check` 通过。
- 已知边界：当前 UI 打开时尚未切换角色协调状态，鼠标点击 UI 仍可能产生 Attack 输入；应在后续统一做「UI 模态状态 → 战斗输入阻断」，不在输入 Provider 内硬编码 UI 判断。

**第 3 步完成：攻击冷却与近战攻速倍率**

- `WeaponConfig` 新增 `attackInterval`（默认 0.5 秒、最小 0.01 秒），每种武器独立配置基础攻击间隔；旧资产字段异常为 0 时由控制器回退到 0.5 秒。
- `EquipmentController` 使用长度为 3 的 `_nextAttackAllowedTimes` 保存每个槽位的下次攻击时间：切换到另一槽不会清除原槽冷却，也不会让另一把武器继承当前武器冷却。
- `CanAttack` 现统一检查角色全局状态、切换状态、当前武器/行为及当前槽冷却；`Attack()` 改为返回成功与否，只有实际执行攻击后才开始冷却。
- 近战实际间隔公式：`baseInterval / Combat_MeleeAttackSpeedMultiplier`；例如基础 0.5 秒、倍率 1.25，实际间隔 0.4 秒。远程武器不受近战攻速加成影响。
- 同槽替换为不同武器时清除旧武器冷却；重复拾取同一配置不会重置冷却，避免绕过限制。
- `CombatDebugMenu` 的状态输出新增当前攻击间隔和剩余冷却，便于回家验证加成前后数值变化。
- 武器模式决策补充：近战只有锯子这一把专武，固定按下沿触发单次攻击；枪械通过 `WeaponConfig.isAutomatic` 区分全自动/半自动。半自动枪和锯子点击一次攻击一次；全自动枪在按住左键时持续请求攻击，由 `attackInterval` 冷却决定实际射速。即使误勾锯子的 `isAutomatic`，装备层也会因其类型为 Melee 而忽略。
- 验证：静态检查 PASS（pass=3 fail=0 skip=1）、代码诊断 0、`git diff --check` 通过。运行时需回家为锯子/枪的 `WeaponConfig.attackInterval` 对照动画时长调参，并按枪械类型勾选 `isAutomatic`。

**第 4 步完成：UpperBody 状态机接口与动画扩展点**

- 状态模型：持续姿态仅设 `Inactive` / `RangedReady`；瞬时动作定义 `Fire` / `Reload` / `UseItem`。没有把开火做成等待动画结束的状态，避免动画时长、Animation Event 反向控制玩法冷却。
- 动画解耦：新增 `IUpperBodyAnimationSink` 语义接口，状态机只调用 `SetUpperBodyMode` / `PlayUpperBodyAction`，不含 Animator 参数名、Layer 名、状态名或动画片段引用；Sink 不存在时仍可正常装备、攻击和结算伤害。
- 状态切换：装备枪械进入 `RangedReady`，装备锯子或空手进入 `Inactive`；全局状态禁攻时临时抑制为 `Inactive`，恢复后按保留的请求姿态自动恢复。
- 事实路由：`EquipmentController` 新增角色实例级 `WeaponEquipped` / `AttackCommitted` 事件；`CharacterRoot` 负责把装备事实映射为持续姿态，把成功的远程攻击映射为 `Fire`。冷却、切换或角色状态阻断的攻击不会误播开火动作；锯子攻击不走 UpperBody Fire，未来接 FullBody 攻击表现。
- 事件边界：保留全局 `EventBus.Weapon_Equipped` 供 UI/日志观察；角色内部表现用实例事件，避免未来多角色时所有 UpperBody 同时响应。
- `CombatDebugMenu` 新增 UpperBody 当前模式/抑制状态输出，可在没有动画资源时验证语义链路。
- 本步未新增脚本文件，复用已有 `UpperBodyStateMachine.cs`，因此公司端不会产生新的缺 `.meta` 问题；`WeaponConfig.animPoseParam` 暂时保留兼容旧资产，但状态机不读取它。
- 验证：UpperBody 不引用 `EquipmentController`、攻击行为或冷却；`EquipmentController` 不引用 UpperBody/Animator；静态检查 PASS、代码诊断 0、`git diff --check` 通过。

**回家对接清单（本轮四步）**

- 让 Unity 重新导入 `PlayerInputActions.inputactions`，确认生成的 `PlayerInputActions.cs` 与仓库版本一致且编译 0 error。
- 锯子 `WeaponConfig`：设置合适的 `attackInterval`，`isAutomatic` 无需勾选（即使误勾也会被 Melee 类型忽略）。
- 枪械 `WeaponConfig`：按半自动/全自动决定 `isAutomatic`，并按射速填写 `attackInterval`。
- Play Mode 验证：1/2/3 空槽忽略与槽位切换、切换中禁攻、三槽独立冷却、同槽换枪、锯子攻速加成、半自动点击/全自动长按。
- 用 `Tools/战斗调试/打印战斗状态` 查看武器模式、实际攻击间隔、剩余冷却及 UpperBody 模式。
- 当前无需配置 Animator 参数或 Avatar Mask；找到动画资源后再实现 `IUpperBodyAnimationSink` 适配器。

**本日收尾**

- 四项计划全部完成：`EquipmentController` 稳定性、战斗输入、冷却/攻速/全半自动、UpperBody 接口层。
- 公司端最终检查：`static_check.ps1` PASS（pass=3 fail=0 skip=1）、代码诊断 0、`git diff --check` 通过；Input Action 源 JSON 可解析、与生成包装器内嵌 JSON 一致、30 个 Action/Binding ID 无重复。
- 未验证边界：公司端无 Unity 与 ScriptableObject/Animator 资源，编译、Inspector 配置和 Play Mode 行为按上方「回家对接清单」验证。
- 已知后续项：UI 打开时统一阻断战斗输入；实现 Animator Sink 与武器模型挂载；补测试 `IDamageable`、近战目标去重/自身过滤、枪械瞄准方向。
- Git 计划分两批提交：①忽略 `.codebuddy/memory/`；②本日战斗输入、冷却、枪械模式、UpperBody 接口及开发日志。

**回家对接情况**

- ✅ 跑通的：
  - `run_check.ps1` **ALL PASS**：Static 4/4、Compile **warnings=0**、Scene 6/6——公司端无 Unity 无法验证的编译项已补齐。
  - Input Actions 重新导入后生成的 `PlayerInputActions.cs` 与仓库版本一致（`git status` 干净，未被重新生成覆盖）。
  - **Play Mode 6 项全部通过**：① `1/2/3` 空槽忽略与槽位切换 ② 切换武器期间（0.3s）禁攻 ③ 三槽独立冷却互不继承 ④ 同槽换枪清除旧冷却 ⑤ 锯子攻速加成生效（间隔按倍率缩短）⑥ 半自动点击 / 全自动长按行为正确。
  - 两个武器资产补上 `attackInterval` / `isAutomatic` 字段（旧资产 YAML 无此键，Unity 不给已有资产回填 C# 默认值，会反序列化为 0；代码虽有 `DEFAULT_ATTACK_INTERVAL=0.5` 兜底不崩，但 Inspector 显示 0 无法调参）。
- ⚠️ 要改的：
  - 无代码改动。
  - 武器资产最终值：锯子 `attackInterval 0.6` / `isAutomatic 0`；枪 `attackInterval 0.25` / `isAutomatic 0`（半自动）。均为手感起点值，**待接上攻击动画后再按动画时长对齐**。
  - 角色动画/模型资源未定：可琳动画包为 Generic 骨骼且**无持枪动作**，无法同时满足「持枪 + 近战挥砍」，已决策更换角色（见下节）。
  - 可选 `Bonus_Used` 订阅弹提示：仍未做。
- 🐛 新踩的坑：
  - **db 副本只在首次运行时拷贝**：`SqliteManager.Init()` 仅当 persistentDataPath 副本**不存在**时才从 `StreamingAssets` 拷贝。本次未删副本但加成道具仍生效，是因为改源库（09-01 晚）发生在首次运行**之前**；**以后再改源库必须删副本** `C:\Users\Fall_night\AppData\LocalLow\DefaultCompany\Project\game.db`，否则改动不生效。
  - **Play Mode 内的 Inspector 改动会丢失**：资产调参须在编辑模式进行并保存，Play Mode 里改的值退出即回退。
  - **查场景/预制体组件要用脚本 GUID**：场景/预制体按 GUID 引用脚本，搜类名文本搜不到，须读 `.cs.meta` 取 guid 再搜（一度误判「角色没挂 `EquipmentController`」，实际已挂）。

**动画资源方案（本日决策）**

- **决策**：更换角色模型为 **Mixamo 角色**。原因：可琳动画包为 Generic、无持枪动作，而项目需「持枪（远程）+ 近战挥砍」两套动作。
- **代价可控**：角色为代码位移（`CharacterController.Move` + `OnAnimatorMove()` 丢弃 Root Motion），Animator 只吃 `Speed` / `IsGrounded`，武器系统表现层已解耦 → 换模型**不动任何战斗代码**，只需换 Mesh / Avatar，并**重拖 `rightHandAttachPoint`** 到新角色右手骨骼。
- **动画分层判断**：枪械待机 `RangedReady` / 射击 `Fire` / 换弹 `Reload` 走 **UpperBody Layer**（Avatar Mask 遮上半身，下半身继续移动）；**近战挥砍走 Base Layer（全身）**——挥砍需腰腿发力，只遮上半身会很怪。UpperBody 侧语义已就绪（差 `IUpperBodyAnimationSink` 适配器）；近战需在 Base Layer **新增 Melee Attack 状态**（Locomotion → Melee Attack → Locomotion，Trigger + Exit Time），这是当前缺口。
- **Mixamo 工作流**：角色 fbx 选 T-pose + **With Skin**（`Create From This Model`）；动画 fbx 全部 **Without Skin**（`Copy From Other Avatar`，Source 指角色 Avatar）；**角色与所有动画须来自同一个 Mixamo 角色**，骨骼一致才能 retarget 零误差。
- 所需动画：枪械 `Idle / Walking / Running Rifle`、`Firing Rifle`、`Reloading`；近战 `Standing Melee Attack Horizontal` + `Downward`（可做连招）。
- 当前 Animator 状态：`PlayerAnimator.controller` 仅 1 个 Base Layer + 1 个 Blend Tree（Idle/Walk/Run），参数仅 `Speed` / `IsGrounded`，**无攻击状态、无 UpperBody Layer、无 Avatar Mask**（全项目 0 个 `.mask`）。

### 2026-09-03｜武器地图掉落拾取入口（舍弃制造）

**方向调整**：枪械原计划「地图拾取 + 制造」双来源，经确认后**舍弃制造**——武器不物品化（不进 `ItemConfig` 表、无 itemId），仅纯地图掉落；同槽冲突改为**拒绝并提示**（不覆盖旧武器）。

- `EquipmentController`：
  - 新增 `HasWeaponInSlot(WeaponSlot)`：查询槽位是否已有武器。
  - 新增 `TryPickup(WeaponConfig, out string failReason)`：运行时拾取入口，槽位已占用时拒绝并返回原因；空槽才落槽并切换。
  - 原 `Pickup` 保留「同槽覆盖」语义，仅留给 Editor 调试菜单（`CombatDebugMenu`）用。
- `CharacterRoot`：新增 `Equipment` 公开属性，暴露 `equipmentCtrl`，供掉落物解析装备控制器。
- 新增 `Interaction/WeaponPickupItem.cs`：实现 `IInteractable`，持有 `WeaponConfig` 引用；交互后走 `TryPickup` 进专属三槽，成功后 `Interaction_Performed` + 销毁；失败提示玩家「该槽位已装备武器，请先丢弃旧武器」。
- 补 `WeaponPickupItem.cs.meta`（guid `6be64a2bdec449599fe9d417719894f0`）。

**统一浮动提示**

- 新增 `EventName.UI_Toast` + `View/ToastView.cs`：常驻 Canvas 下的全局提示组件，订阅字符串消息，以 unscaled time 播放淡入、上飘、淡出；连续消息会打断并重播，Text 不接收射线。
- `CraftView` 删除私有 `tipText` / `ShowTip` / `TipRoutine` 重复实现；制作失败改发 `UI_Toast("材料不足")`。
- `PickupItem` 背包满、`WeaponPickupItem` 槽位占用均改发 `UI_Toast`；武器失败不再覆盖持续性的 `Interaction_TargetChanged` 交互提示。
- `SceneAudit` 移除已废弃的 `CraftView.tipText` 检查，新增 `ToastView` 存在性检查。回家需在**不会随背包/制作面板关闭的常驻 Canvas** 下创建 Text 并挂 `ToastView`；不要把 Text 自身设为 inactive。
- 补 `ToastView.cs.meta`（guid `4aa537a047d6406a90b53d9a6a860bcb`）。静态检查 PASS（pass=3 fail=0 skip=1）。

**丢弃武器（补闭环）**

- 背景：`TryPickup` 同槽拒绝提示「先丢弃旧武器」，但丢弃入口缺失会死锁；本次补齐 G 键丢弃。
- `EquipmentController` 新增 `Drop()`：丢弃当前武器，清空槽位与冷却、打断切换、`Blackboard.Remove("Weapon_CurrentType")`、复用 `Weapon_Equipped(old, null)` 让表现层回空手，广播 `Weapon_Dropped`。
- 丢弃时在玩家前方地面动态生成 `WeaponPickupItem` 掉落物：贴地 + `SphereCollider`(trigger) + 可选 `modelPrefab`；无模型时逻辑仍可交互拾取（视觉待资源）。
- `WeaponPickupItem` 新增 `SetWeaponConfig` 供运行时注入；`EventName` 新增 `Weapon_Dropped`。
- 输入链路：`IInputProvider.DropWeaponPressedThisFrame` + `PlayerInputProvider.OnDropWeapon` + `PlayerInputActions`/`.inputactions` 同步新增 `DropWeapon` action（G 键）+ `CharacterRoot` 路由。

**回家对接清单（本日需在 Unity 完成）**

- 常驻 Canvas 下创建 Text 并挂 `ToastView`（不随背包/制作面板关闭，Text 保持 Active）。
- 场景武器 GameObject 挂 `WeaponPickupItem` 并拖入对应 `WeaponConfig`（锯子/枪）。
- 给 `WeaponConfig` 填 `modelPrefab`，否则丢弃的掉落物不可见（仍可交互拾取）。
- 运行 `SceneAudit` 确认 `ToastView 存在` 检查通过。

**回家对接情况**

- ✅ 跑通的：
  - Play Mode 全链路通过：走近武器出现交互提示 → 按 E 拾取进对应槽位；按 G 丢弃 → 玩家前方地面生成掉落物且可再次拾取；槽位已占用时拾取被拒绝并弹出 Toast 提示。
  - 补挂 `InteractionDetector`：角色上原本缺少交互检测器，补挂后交互链路恢复（详见下方踩坑）。
  - `ToastView` 挂到常驻 Canvas 下，全局浮动提示生效（`UI_Toast` 事件驱动，制作失败/背包满/槽位占用均可触发）。
  - 场景武器 GameObject 挂 `WeaponPickupItem` + `weaponConfig` 已拖入 + Collider 就位。
- ⚠️ 要改的：
  - 无代码改动。
  - `WeaponConfig.modelPrefab` 仍未填（锯子/枪两个资产均为空）：掉落物与丢弃生成的掉落物暂不可见，但可正常交互拾取。等武器模型资源到位后补。
  - SceneAudit 的 `ToastView 存在` 检查本次未能跑到（用户编辑器占用，`run_check` 互斥保护正确 STOP），待关闭编辑器后补验。
- 🐛 新踩的坑：
  - **角色上漏挂 `InteractionDetector`**：这是交互系统的检测器（`Physics.OverlapSphereNonAlloc` 检测 + 按 E 触发）。缺失时表现为「靠近无任何提示、按 E 无反应」，**不报错、不警告**，只能靠功能缺失发现——属于「静默失效」类问题，值得在 SceneAudit 增加对应检查项。
  - **按脚本 GUID 搜索场景 YAML 的结果不可全信**：本次试图用脚本 GUID 判断角色是否就位，`CharacterRoot` / `PlayerInputProvider` / `EquipmentController` / `InteractionDetector` 四个 GUID 在场景 YAML 中均命中 0，一度误判「场景里没有角色」；实际角色一直在场景里（用户实证）。大场景文件的文本搜索存在盲区，**涉及「场景里有没有某对象」的判断，应以 Unity Hierarchy 为准，不能仅凭 YAML 搜索下结论**。
  - `run_check.ps1` 在编辑器打开时会正确 STOP（互斥保护生效），此时编译/场景验证应依赖编辑器 Console 热编译，不要强行 `-Force`。

---

### 2026-09-04｜UI 模态阻断战斗输入

**背景**：打开背包/制作面板时，角色仍可攻击/切枪/丢弃，需在 UI 打开时阻断战斗输入，走统一模态而非输入层硬编码。

**实现**

- 新增 `Core/IModalPanel.cs` 标记接口：实现该接口的面板打开即进入「UI 模态」；非模态面板（HUD/Toast）不实现，不影响战斗输入。
- `UIManager`：新增 `_activeModalPanels` 集合；`OpenPanel`/`ClosePanel` 对 `IModalPanel` 面板更新集合，并在「无模态→有模态」「有模态→无模态」时广播 `EventName.UI_ModalChanged(bool)`。
- `BagView`/`CraftView` 实现 `IModalPanel`。
- `CharacterRoot`：订阅 `UI_ModalChanged` 维护 `_uiModalOpen`；`HandleCombatInput` 开头 `if (_uiModalOpen) return;` 阻断攻击/切枪/丢弃；`OnDestroy` 释放订阅 token。
- 补 `IModalPanel.cs.meta`（guid `f4a1fa28ac0b4c4e9d0dedc63711a33c`）。

**说明**

- 阻断战斗输入（攻击/切枪/丢弃）与移动输入：`HandleCombatInput` 与 `fullBodySM.OnUpdate()` 均在 UI 模态打开时跳过，角色完全站住。
- 事件驱动 + 标记接口：未来新增模态面板只需实现 `IModalPanel`，无需改 CharacterRoot 或输入层。
- 静态检查 PASS（pass=3 fail=0 skip=1）。

---

### 2026-09-07｜战斗命中完善

**背景**：战斗骨架（三槽/冷却/全半自动/拾取/丢弃/UI 模态）已闭环，但「能挥能射、打不到东西」——近战/枪械命中判定缺少自身过滤、去重与瞄准方向，也没有可命中的测试目标。

**改动**

- `IWeaponBehavior.Attack` 签名新增 `Vector3 aimDirection` 参数：近战忽略（沿角色面向挥砍），枪械沿瞄准方向发射。
- `WeaponConfig` 新增 `LayerMask hitMask = ~0`：可命中目标层掩码，回家可配置为 Enemy/可破坏物层以过滤地面/UI。
- `MeleeWeaponBehavior`：
  - 自身过滤：`hit.transform.IsChildOf(attacker)` 跳过角色自身碰撞体，杜绝自伤。
  - 同目标去重：`HashSet<IDamageable>` 保证同一根对象多 Collider 只扣一次血。
  - `Physics.OverlapSphereNonAlloc` + 复用 `Collider[32]` 缓冲，零每帧 GC。
  - 接入 `weapon.hitMask`。
- `RangedWeaponBehavior`：
  - 射线方向改用 `aimDirection`（相机准星），无有效方向时回退 `attacker.forward`。
  - 自身过滤 + `weapon.hitMask`。
- `CharacterRoot` 新增 `GetAimDirection()`：返回 `inputProvider.LookDirection`（世界空间、归一化），无输入回退 `transform.forward`。
- `EquipmentController.Attack()` 计算并传入瞄准方向。
- 新增 `Combat/DummyTarget.cs`（+ meta，guid `d0a1b2c3e4f5061728394a5b6c7d8e9f`）：实现 `IDamageable` 的最简可命中假人；`maxHealth`/`destroyOnDeath` 可配，`Damaged(float 剩余血量)`/`Died` 实例事件供反馈订阅，无 Collider 自动补 `SphereCollider`，`ResetHealth()` 复活。

**验证结果**

- `static_check.ps1` PASS（pass=3 fail=0 skip=1）；代码诊断 0；`git diff --check` 通过。
- 公司端无 Unity，运行时命中验证待回家（挂 DummyTarget + 配置 hitMask）。

**回家对接清单**

- 场景新建假人 GameObject 挂 `DummyTarget`（可拖入现有 Collider，不拖则自动补球体），验证近战/枪械命中与扣血、同目标去重、自身不受伤。
- `WeaponConfig.hitMask` 按需配置（默认 ~0 全部；建 Enemy 层后可过滤地面/UI）。
- 枪械准星方向现走 `Camera.main.transform.forward`，待接枪口挂点后改为枪口起点 + 准星射线。

**回家对接情况**

- ✅ 跑通的：
  - 场景挂 `DummyTarget` 假人后，**近战命中通过**：调试菜单装备锯子 → 靠近假人攻击 → 假人扣血并在归零时销毁（`destroyOnDeath` 生效）。
  - **枪械命中通过**：调试菜单装备枪 → 远距离射击可命中假人。
  - 命中链路（`IDamageable` → 伤害结算 → 死亡销毁）完整闭环。
- ⚠️ 要改的：
  - 无代码改动。
  - **同目标去重未验证**：当前假人为单一 Collider，需构造多 Collider 目标才能观察去重效果。
  - **自身过滤暂无法验证**：角色尚无血量系统，自伤看不出来——待第一阶段 `HealthController` 落地后回归。
  - `WeaponConfig.hitMask` 保持默认 `~0`，待建 Enemy 层后配置。
- 🐛 新踩的坑：
  - 无。但澄清一点：实测枪械瞄准「看似跟随角色朝向」——实际 `PlayerInputProvider.LookDirection` 返回的是
    **`Camera.main.transform.forward`**（相机朝向），因第三人称相机跟随角色，两者视觉上一致，属**预期行为而非 bug**。
    准心（屏幕准星 UI）尚未制作，待准心落地后射线改为「枪口起点 + 准星射线」。

**下一步**

- 完成 `DummyTarget` 与近战/枪械命中链路对接验证（本次已完成）。
- 明天开始敌人 AI 前置基础重构，按下方已确认方案分阶段动工。
- 表现层：`IUpperBodyAnimationSink` 适配器 + 武器模型挂载 + UpperBody Layer/Avatar Mask（回家）。

### 2026-09-07｜敌人 AI 长期方案评估与决策（计划，明日开工）

**项目定位与结论**

- 项目不只用于毕设，敌人 AI 按可长期扩展的产品级方向设计，不把目标限制在 `Idle → Chase → Attack` 演示型逻辑。
- 接受长期分层方案：**HFSM + Utility AI + A* 导航 + Influence Map 战术代价 + Context Steering/RVO 局部避障**。
- 上述能力不一次性落地：先完成可玩的垂直切片，再按真实玩法需求逐层增强，避免过度设计。
- FSM 与 A* 不冲突：FSM/HFSM 决定「做什么」，A* 决定「怎么走到目标」，Influence Map 调整路线代价，Context Steering/RVO 负责局部避障。
- 具体 A* 实现暂不锁定。候选包括第三方 Recast/Grid Graph、自研图搜索或其他导航图；待回家结合真实场景地形、动态障碍比例、敌人数量与地图规模后再最终选型。

**为什么适合当前项目**

- 已有 `IInputProvider`、FullBody 状态机、`CharacterController`、`EquipmentController`、`IDamageable`、`CharacterStateCoordinator`，敌人可复用角色移动与战斗执行链。
- 现有系统只需处理多角色隔离与伤害边界，AI Brain、感知、导航、Utility 评分等主要以新增模块落地，不需要推倒玩家角色系统。
- `CharacterController` 继续作为唯一位移权威；导航层只输出路径、拐点和移动意图，避免与 NavMeshAgent/A* 移动组件争抢 Transform。

**动工前必须解决的结构问题**

1. **Blackboard 实例隔离（中等复杂度）**
   - 当前静态 `Blackboard` 会让玩家/敌人共享 `MoveSpeed`、`Air_VerticalVelocity`、武器类型和攻击加成，任一角色销毁时 `Blackboard.Clear()` 还会清空所有角色数据。
   - 改为每个 `CharacterRoot` 持有实例级 `CharacterRuntimeData`；移动速度、垂直速度、当前武器类型归运行数据。
   - 攻击力/近战攻速等角色属性独立为实例级 `CombatStats`；`BonusController` 只修改玩家属性，敌人使用自己的配置值。
2. **UI 模态玩家隔离（低复杂度）**
   - 只有玩家控制角色响应 `UI_ModalChanged`；敌人不能因玩家打开背包/制作面板而停止行动。
   - 使用显式角色控制身份或等价的抽象判断，不在 AI 中硬编码 UI。
3. **生命、阵营与伤害上下文（中等复杂度）**
   - 新增通用 `HealthController` 与 Player/Enemy 阵营关系，建立玩家与敌人双向受伤/死亡闭环。
   - 趁当前仅 `DummyTarget` 实现 `IDamageable`，评估把 `TakeDamage(float)` 升级为 `DamageContext`，为攻击者、命中点、方向、伤害类型、击退、仇恨与击杀归属预留正式边界。
   - `LayerMask` 负责物理层快速过滤，Faction/Team 负责玩法层友伤判断。

**实际落地范围与顺序**

**第一阶段：多角色安全基础（明日首先实施）**

- 静态 `Blackboard` 改为角色实例级运行数据。
- 分离实例级 `CombatStats`，迁移玩家永久加成读取链路。
- UI 模态只阻断玩家角色。
- 实现 `HealthController`、Faction/Team、正式伤害上下文。
- 玩家与敌人双方伤害、死亡状态闭环。

**第二阶段：可玩的敌人 AI 垂直切片**

- 新增 `EnemyConfig`、`EnemyPerception`、`EnemyBrain`、`AIInputProvider`。
- 首版行为状态：`Idle / Chase / Attack / Dead`。
- 先用 `DirectNavigation` 在无障碍小场地跑通：发现玩家 → 追击 → 进入攻击距离 → 玩家受伤；玩家反击 → 敌人受伤/死亡。
- Brain 只负责决策；感知、导航、攻击执行和生命互相解耦。

**第三阶段：A* 导航接入**

- 建立 `IEnemyNavigation`：设置目的地、停止、移动方向、到达/失败/卡住状态。
- Brain 不直接依赖具体 A*、NavMeshAgent 或第三方组件。
- 选定图类型后实现 A* 导航；路径层只提供移动意图，通过 `AIInputProvider → FullBodyStateMachine → CharacterController.Move()` 执行。
- 增加路径可视化、重新寻路节流、目标移动阈值、路径失败与卡住诊断。

**第四阶段：行为与导航亮点增强**

- HFSM 增加 `Patrol / Investigate / ReturnHome / Stunned`。
- Utility AI 在存活/正常强状态下，根据距离、生命、视线、冷却、危险度选择追击、攻击、撤退、绕后等战术。
- Influence Map 将危险、地形、拥挤、掩体等因素注入 A* 路径代价，使不同敌人选择不同路线。
- Context Steering/RVO 处理路径附近局部避障和群体分流。
- 只有敌人规模和大地图性能证明有必要时，再评估 HPA* 分层寻路或 Flow Field；不提前实现。

**复杂度判断**

- 整体属于「少量必要重构 + 大量新增模块」，不是推倒重写。
- 必要重构集中在 Blackboard、UI 模态隔离、生命/阵营/伤害三块，复杂度中等且当前改动成本最低。
- EnemyBrain、感知、AI 输入、导航接口和后续 Utility/Influence Map 均以新增模块为主，可逐阶段独立验证和提交。

**今晚与明日边界**

- 今晚只完成 09-07 已开发战斗命中内容的 Unity 对接和回归，不提前修改敌人 AI 架构。
- 明天从第一阶段「多角色安全基础」开始，完成并验证后再进入敌人 AI 垂直切片。

### 2026-09-08｜第一阶段：多角色安全基础（Blackboard 实例化 + 阵营/生命/伤害闭环）

**今日完成（代码）**

- 新增实例级数据容器，替代静态全局 `Blackboard`：
  - `Role/Core/CharacterRuntimeData.cs`（+ meta）：每个 `CharacterRoot` 持有一份，存 `moveSpeed`、`airVerticalVelocity` 等瞬时运行状态。
  - `Combat/CombatStats.cs`（+ meta）：实例级战斗属性（`attackMultiplier`、`meleeAttackSpeedMultiplier`），玩家由 `BonusController` 累加，敌人持各自默认值。
- 新增生命/阵营/伤害正式边界（`Combat/` 目录，均含 meta）：
  - `Faction.cs`（`Neutral/Player/Enemy`）与 `IFactionMember.cs`（阵营身份接口）。
  - `DamageContext.cs`（struct：`amount/attacker/sourceFaction/hitPoint/direction`，含静态 `ResolveFaction`）。
  - `HealthController.cs`：实现 `IDamageable`，管理当前/最大生命、`Damaged`/`DamagedWithContext`/`Died` 事件、友伤过滤、`ResetHealth()`。
- `IDamageable.TakeDamage(float)` 升级为 `TakeDamage(DamageContext)`；`MeleeWeaponBehavior`/`RangedWeaponBehavior` 构造并传入完整伤害上下文（攻击者、阵营、命中点、方向）。
- `DummyTarget` 重构为「死亡策略」薄封装：`RequireComponent(HealthController)`，生命逻辑移交 `HealthController`，保留 `destroyOnDeath` 与自动补 Collider。
- `CharacterRoot` 接入：
  - 实现 `IFactionMember`，新增 `Faction` 序列化字段（默认 `Player`）。
  - 持有实例 `Runtime`（`CharacterRuntimeData`）与 `CombatStats`。
  - 新增可选 `HealthController` 引用（未拖拽自动查找），订阅 `Died → Die()`。
  - 新增 `IsPlayerControlled`（默认 true）：只有玩家角色订阅 `UI_ModalChanged`，敌人不因玩家开面板而停止。
- `EquipmentController` 迁移：`Attack()` 读 `_character.CombatStats.attackMultiplier`；`GetEffectiveAttackInterval` 改实例方法，近战攻速倍率读角色实例 `CombatStats`；移除 `Weapon_CurrentType` 死数据。
- `BonusController` 迁移：新增 `SetCombatStats()` 注入，`ApplyBonus` 累加到玩家实例 `CombatStats`；移除静态 `Blackboard` 依赖。
- `GameRoot`：`Update` 中延迟查找玩家 `CharacterRoot`（按 `IsPlayerControlled`）并把其 `CombatStats` 注入 `BonusController`（首次非 null 后缓存）。
- 5 个 FullBody 状态（Idle/Walk/Run/Jump/Fall）迁移到 `character.Runtime`，清理多余 `using Role.Core`。
- 删除 `Combat/CombatKeys.cs`、`Role/Core/Blackboard.cs`（及各自 meta），静态全局容器彻底下线。
- `CombatDebugMenu` 调试输出改读玩家实例 `CombatStats`，不再依赖 `Blackboard`。

**验证结果**

- `static_check.ps1` PASS（pass=3 fail=0 skip=1）；代码诊断 0；`git diff --check` 通过。
- 公司端无 Unity，运行时验证待回家（玩家/敌人挂 `HealthController` 后做双向伤害回归）。

**回家对接清单**

- 玩家 `CharacterRoot` 挂 `HealthController`（`Faction=Player`）；敌人/假人挂 `HealthController`（`Faction=Enemy` 或保持 Neutral）。
- 验证玩家打敌人扣血与死亡、友伤过滤（同阵营不掉血）、自身过滤、`DummyTarget` 多 Collider 去重。
- `WeaponConfig.hitMask` 建 Enemy 层后按需配置。

**回家对接情况**

- ✅ 跑通的：
  - 玩家 `Player.prefab` 与场景假人均补挂 `HealthController`（升级前两者都缺）。
  - 玩家攻击假人可正常扣血并触发死亡（假人无 `CharacterRoot` → 阵营回退 `Neutral`，不参与友伤判定，谁都能打中）。
  - **玩家攻击自身不掉血**：近战 `IsChildOf(attacker)` 自身过滤 + `HealthController` 同阵营友伤过滤双重生效。
  - 编译与场景自检通过：`run_check.ps1` ALL PASS（Static 4/4、Compile warnings=0、Scene 8/8）。
- ⚠️ 要改的：
  - 无代码改动。
  - **同目标去重未验证**：当前假人仅单一 Collider，去重逻辑不触发；待第二阶段敌人模型（多碰撞体）再验证。
  - **玩家受伤/死亡未验证**：暂无可攻击玩家的目标。`Die()` 只切协调器 `CharacterState.Dead`
    （`CanMove`/`CanAttack` 均返回 false），逻辑层已闭环但无 `DeadState` 与死亡表现，
    待第二阶段敌人 AI 或死亡表现层落地后一并验证。
- 🐛 新踩的坑：
  - **`[RequireComponent]` 不会给已存在的实例补组件**：场景假人挂的是旧版 `DummyTarget`（当时无该约束），
    脚本升级后仍缺 `HealthController` → `Awake` 中 `GetComponent` 返回 null、不订阅 `Died`
    → **打不死且不报错**（静默失效）。`[RequireComponent]` 只在**添加组件的那一刻**生效，
    后续为脚本新增该约束时，已存在的实例必须手动补挂或移除重加。

**下一步**

- 进入第二阶段：敌人 AI 垂直切片（`EnemyConfig`/`EnemyPerception`/`EnemyBrain`/`AIInputProvider` + `Idle/Chase/Attack/Dead` + `DirectNavigation`）。

### 2026-09-08｜补充优化：CharacterContext 收敛 CharacterRoot 职责

针对「CharacterRoot 上帝对象化」隐患做一轮收敛——把分散暴露的实例数据字段收进一个共享上下文。

- 新增 `Role/Core/CharacterContext.cs`（+ meta）：聚合 `CharacterRuntimeData Runtime` + `CombatStats CombatStats`，由 `CharacterRoot` 在 `Awake` 组装，注入给状态机与各控制器。
- `CharacterRoot` 收敛：移除 `Runtime`、`CombatStats` 两个独立 public 属性，改为单一 `public CharacterContext Context`；根节点只负责「组装 + 注入」，不再逐个暴露字段。
- 迁移全部调用方到 `character.Context`：
  - 5 个 FullBody 状态（Idle/Walk/Run/Jump/Fall）读写 `character.Context.Runtime.moveSpeed/airVerticalVelocity`。
  - `EquipmentController` 读 `_character.Context.CombatStats.attackMultiplier/meleeAttackSpeedMultiplier`。
  - `GameRoot` 注入 `_playerCharacter.Context.CombatStats` 到 `BonusController`。
  - `CombatDebugMenu` 调试输出读 `character.Context.CombatStats`。
- `BonusController.SetCombatStats()` 签名不变（仍接收 `CombatStats`），调用方改从 `Context` 取。

**验证结果**

- `static_check.ps1` PASS（pass=3 fail=0 skip=1）；代码诊断 0；`git diff --check` 通过。

**设计取舍**

- 现阶段只有 4 个强类型字段（移动速度、空中速度、攻击倍率、攻速倍率），用聚合的强类型 `Context` 比 key-value 黑板更安全、更内聚，不构成过度设计。
- 未来敌人 AI 感知/决策层需要交换异构动态数据（目标、视线、威胁、最后已知位置、冷却）时，可在 `CharacterContext` 内扩展实例级类型安全数据，无需退回全局黑板。

---

### 2026-09-09｜第二阶段：敌人 AI 垂直切片（EnemyBrain + AIInputProvider + 感知/导航）

**今日完成（代码）**

- 新增 `Enemy/` 目录（均含 meta），核心思路：**敌人复用 CharacterRoot，通过 IInputProvider 与玩家同构**，不新建 EnemyRoot、不重复移动/战斗逻辑。
  - `EnemyConfig.cs`：AI 决策参数 ScriptableObject（`detectionRange`/`loseTargetRange`/`attackRange`/`attackCooldown`/`obstacleAvoidDistance` + 初始近战 `meleeWeapon` 引用）。
  - `AIInputProvider.cs`：实现 `IInputProvider`，把 `EnemyBrain` 的决策输出翻译成「移动方向 + 攻击意图」，交给 `CharacterRoot` 已有的移动/战斗链路消费。
  - `EnemyPerception.cs`：距离检测目标（`IsPlayerControlled` 玩家），锁定后缓存；目标死亡脱锁、超出 `loseTargetRange` 脱锁（迟滞防抖）。
  - `DirectNavigation.cs`：直线导航——朝目标水平移动，前方射线命中障碍时向右侧偏转绕过（最简实现，第三阶段替换为 `IEnemyNavigation` + A*）。
  - `EnemyBrain.cs`：决策核心，`EnemyAIState { Idle/Chase/Attack/Dead }`，感知 → 决策 → 输出；死亡优先停止一切行为。
- 关键设计点：
  - **攻击边缘触发时序**：`AIInputProvider` 不在 `LateUpdate` 自动清除攻击标志，改由 `EnemyBrain` 每帧决策前重置、决策后设置，避免「AI 设置 → CharacterRoot 读取」之间的脚本执行顺序竞态（延迟 0~1 帧但不丢失）。
  - **近战命中方向依赖 `attacker.forward`**：`Attack` 状态先用 `CharacterRoot.RotateToward` 转向目标再请求攻击，保证命中朝向正确。
  - **攻击节拍分层**：`EnemyBrain` 用 `attackCooldown` 控制 AI 层攻击请求，`EquipmentController` 自身的武器 `attackInterval` 控制实际攻击频率，两层各司其职。
  - **死亡闭环**：`HealthController.Died → CharacterRoot.Die() → coordinator.Dead` 停移动；`EnemyBrain` 检测 `Health.IsDead` 切 `Dead` 停 AI 输出。

**验证结果**

- `static_check.ps1` PASS（pass=3 fail=0 skip=1）；代码诊断 0；`git diff --check` 通过。
- 公司端无 Unity，运行时验证待回家。

**回家对接清单**

- 搭建敌人 Prefab：`CharacterRoot`（`IsPlayerControlled=false`、`Faction=Enemy`）+ `CharacterController` + `AIInputProvider` + `EnemyBrain` + `HealthController` + `EquipmentController`，并挂 `CharacterConfig` 与 `EnemyConfig`。
- `EnemyConfig`：拖入近战 `meleeWeapon`（近战 `WeaponConfig`），按需调 `detectionRange`/`attackRange`/`attackCooldown`。
- 验证闭环：敌人发现玩家 → 追击 → 进入攻击距离 → 玩家受伤；玩家反击 → 敌人受伤/死亡 → 停止 AI。
- `WeaponConfig.hitMask` 需含 Player 层（玩家 `HealthController` 实现 `IDamageable` 才会被命中）。

**回家对接情况**

- ✅ 跑通的：
  - 搭建敌人 Prefab：`CharacterRoot`（`Faction=Enemy`、`IsPlayerControlled=false`、`Config` 复用 `Player.asset`）
    + `CharacterController` + `AIInputProvider` + `EnemyBrain`（`config` → `EnemyConfig_Default`）
    + `HealthController` + `EquipmentController`。
  - **正向链路**：敌人发现玩家 → 追击 → 进入攻击距离 → 攻击 → 玩家受伤并死亡（不可移动、不可攻击）。
  - **反向链路**：玩家反击 → 敌人扣血 → 死亡 → 停止 AI（不再追击/攻击，原地静止）。
  - 双向伤害死亡闭环成立：玩家与敌人各持实例 `HealthController` / `CombatStats`，互不干扰；
    UI 模态仅阻断玩家，敌人不受影响。
  - 编译与场景自检：`run_check.ps1` ALL PASS（Static 4/4、Compile warnings=0、Scene 8/8）。
- ⚠️ 要改的：
  - 无代码改动。
  - **死亡后动画状态机停在死前状态**：死后 `CanMove=false` → `fullBodySM.OnUpdate()` 不再调用，
    状态机保留最后的 `Walk`/`Run`，表现为「原地播放移动动画」。逻辑正确（不能动、不能攻击），
    属表现缺失，待优先级 6 表现层（死亡动画 / `DeadState`）一并处理；
    临时改善可在 `CharacterRoot.Die()` 中复位状态机（如 `fullBodySM.ToIdle()`）。
  - **敌人不会绕障**：`DirectNavigation` 仅「单根前射射线 + 命中固定右偏 0.8」，
    遇长墙持续蹭墙、遇墙角/凹形易卡死。符合第二阶段「无障碍小场地跑通」定位，
    真正寻路待第三阶段 `IEnemyNavigation` + A*（含重算节流与卡住诊断）。
- 🐛 新踩的坑：
  - **`git pull` 后 Unity 不识别新增脚本（Asset Database 与文件系统脱钩）**：
    `EnemyBrain.cs` / `AIInputProvider.cs` 在磁盘与文件管理器均存在，但 Project 窗口只显示另外 3 个脚本，
    `Refresh`（Ctrl+R）与 `Reimport All` **均无效**，Add Component 搜不到。
    根因是 `Library` 中的 Asset Database 缓存了「该文件不存在」的旧索引（针对具体路径）。
    **解决：关闭 Unity → 删除整个 `Library` 目录 → 重开**，强制全量重建（首次导入较慢但彻底）。
    中途尝试「重命名文件触发重新发现」虽能让 Unity 识别，但 **MonoBehaviour 文件名必须与类名一致**，
    改名后 Add Component 依旧挂不上，仍须改回原名——绕路方案不可取，直接删 `Library` 更省事。
  - 项目当前**仅一份 `CharacterConfig`**（`Assets/ScriptableObject/Player.asset`），敌人暂复用之；
    如需差异化移动参数，另建一个 `CharacterConfig` 资产即可。

**下一步**

- 第三阶段：A* 导航接入（`IEnemyNavigation` 抽象 + 具体 A* + 路径可视化/重算节流/卡住诊断）。

---

### 2026-09-10｜第三阶段：A* 网格导航（接口化 + 诊断闭环）

**今日完成（代码）**

- 新增 `Enemy/Navigation/`（含目录 meta 与各脚本 meta）：
  - `IEnemyNavigation.cs`：统一导航契约，包含 `Idle/Moving/Reached/Failed/Stuck` 状态与明确失败原因；导航只输出世界空间移动方向，不直接改 Transform。
  - `EnemyNavigationGrid.cs`：场景级 2.5D 单层 XZ 网格；向下 Raycast 采样地面、Capsule 检查角色净空，支持坡度/台阶限制、八方向邻接与禁止对角穿墙角。
  - `GridAStarPathfinder.cs`：A*（Octile 启发、索引二叉堆、固定数组缓冲、共线节点压缩），重复寻路不创建 Node/List/Dictionary。
  - `GridAStarNavigation.cs`：每敌人路径跟随、目标移动阈值、重算节流、失败降频重试、卡住检测、路径 Gizmos。
- `EnemyBrain` 改为依赖 `IEnemyNavigation`：显式 `navigationSource` 优先，其次自动查找组件，旧 Prefab 无 A* 时回退 `DirectNavigation`；Brain 主动按 `SetDestination → Tick → MoveDirection` 驱动，不依赖跨组件 Update 顺序。
- `DirectNavigation` 实现同一接口并补显式 `navigationObstacleMask` + `QueryTriggerInteraction.Ignore`，保留旧 Prefab 兼容。
- `EnemyConfig` 增加 A* 重算、停止、路径点、搜索上限、卡住诊断参数；`EnemyPerception` 无目标搜索节流为 0.25 秒，降低待机 GC 频率。
- `CharacterRoot` 增加只读 `CanMove`，Brain 在死亡/眩晕等禁移状态暂停导航 Tick，避免误报卡住。
- 修复关键边界：
  - 最终路径段进入攻击停止距离即可到达，避免敌人与玩家 Collider 相撞后永远卡在 Chase；仍要求走到最后路径段，避免隔墙近距离误攻击。
  - A* 终点吸附到邻近可走格后，走完实际路径即结束，不无限重算同一路径；Brain 仍以 `attackRange` 决定是否攻击。
  - 对角移动同时校验两个正交侧格的台阶高度；修正 `maxSearchNodes` 一位偏差。
  - `EnemyBrain` 设 `DefaultExecutionOrder(-50)`，确保 AI 意图先于默认顺序的 `CharacterRoot.Update` 消费。
- `SceneAudit` 新增敌人 AI/A* 检查：敌人身份与阵营、必需组件、近战武器、导航源、唯一有效网格、Ground/Obstacle Mask；零目标场景不再假 PASS。

**验证结果**

- `static_check.ps1` PASS（pass=3 fail=0 skip=1）；代码诊断 0；`git diff --check` 通过。
- 公司端无 Unity/场景资产，Unity 编译与运行时 A* 验证待回家。

**回家对接清单**

- 新建 Layer：`WalkableGround`、`NavigationObstacle`（Player/Enemy 不加入静态障碍 Mask）；地面放前者，墙/不可穿越物放后者。
- 场景新增唯一 `EnemyNavigationGrid`：Rotation=0、Scale=1，范围覆盖测试区；Ground/Obstacle Mask 分离；`agentRadius/agentHeight/maxStepHeight/maxSlope` 与敌人 `CharacterController` 对齐。
- 敌人 Prefab 新增 `GridAStarNavigation`，`EnemyBrain.navigationSource` 可拖入该组件（为空也会自动查找）。
- 验收：无障碍追击、长墙/L 形墙绕行、禁止切墙角、移动目标重算、目标移出网格失败诊断、动态阻挡触发 Stuck、路径 Gizmos、隔墙近距离不攻击。
- 运行 `Tools/run_check.ps1`，新增场景自检会校验敌人身份、武器与 A* 配置。

**回家对接情况**

- ✅ 跑通的：
  - 完成 A* 导航 Unity 对接：新建 `WalkableGround` / `NavigationObstacle` 两个 Layer（Player/Enemy 未加入静态障碍 Mask），
    场景新增唯一 `EnemyNavigationGrid`（Rotation=0、Scale=1，范围覆盖测试区），敌人 Prefab 挂 `GridAStarNavigation`。
  - **8 项验收全部通过**：无障碍追击、长墙/L 形墙绕行、禁止切墙角、移动目标重算、
    目标移出网格失败诊断、动态阻挡触发 Stuck、路径 Gizmos、隔墙近距离不攻击。
  - 第二阶段遗留的「敌人蹭墙」问题随 A* 接入解决（`DirectNavigation` 仅为无网格时的回退）。
  - 编译与场景自检 `run_check.ps1` **ALL PASS**：Static 4/4、Compile warnings=0、Scene **10/10**
    （新增「敌人 AI 组件完整」「A* 导航网格配置」两项；未配网格时显示 DirectNavigation 回退，配好后为 A* 生效）。
- ⚠️ 要改的：
  - 无代码改动。
  - 表现层仍未做：死亡动画 / `DeadState` / `IUpperBodyAnimationSink` / UpperBody Layer + Avatar Mask；
    死亡后动画状态机停在死前状态（`Walk`/`Run`）的问题待优先级 6 一并处理。
- 🐛 新踩的坑：
  - A* 对接本身未引入新问题。
  - 备注（包管理）：本机导入 `CombatGirlsCharacterPack` 与 `MagicaCloth2` 后，Unity 自动修改了
    `Packages/manifest.json`（新增 burst / collections / mathematics / shadergraph / render-pipelines.core 等依赖）、
    `Packages/packages-lock.json`，以及 `ProjectSettings/ProjectSettings.asset`
    （`Scripting Define Symbols` 增加 `MAGICACLOTH2`）。
    这两个包按项目策略**不入库**（`.gitignore` 排除 `Assets/*`），故上述配置改动同样**不随代码入库**，
    保持仓库与「只同步代码」的策略一致。

**下一步**

- 完成 Unity 对接回归后进入第四阶段：Patrol/Investigate/ReturnHome + Utility AI + Influence Map + Context Steering/RVO。

---

### 2026-09-10｜瞄准输入接入 + 相机瞄准拉近

**今日完成（代码）**

- 输入契约：`IInputProvider` 新增 `AimHeld`（只表示「按住瞄准键」这一持续状态，拉近/姿态/转向由消费方决定）；
  `PlayerInputActions` 新增 `Aim` 动作（`<Mouse>/rightButton`），并同步手改生成的 `PlayerInputActions.cs`
  （动作与绑定 JSON、`FindAction`、字段/属性、Add/RemoveCallbacks、`IPlayerActions.OnAim` 全部对齐）；
  `PlayerInputProvider` 加 `_aimHeld` 缓存 + `OnAim` 回调（并入 `ResetInputCache`）；`AIInputProvider.AimHeld` 恒 false（接口对称）。
- 瞄准状态：新增 `Role/Core/IAimStateProvider.cs`（含 meta），由 `CharacterRoot` 实现、每帧算 `IsAiming`
  = **持远程武器 且（按住瞄准键 或 在开火后的自动保持期内）**，死亡/眩晕恒 false。与 `AimHeld` 分离（原始输入与角色语义各司其职）。
  开火自动进入瞄准靠 `EquipmentController.AttackCommitted`（只在攻击**真正提交**时触发，冷却/状态阻断不触发）重置计时器，
  时长走 `CharacterConfig.aimAutoHoldSeconds`（1.5）；计算放在 `HandleCombatInput` 之后，保证本帧开火当帧进瞄准。
- 相机拉近（`CameraFollow`）：新增 `aimDistance`(2.5) / `aimSmoothTime`(0.15)，用 `SmoothDamp` 平滑过渡；
  **只拉距离、`height` 不变**（不跟用户已调好的高度打架）。数据源改为 `IAimStateProvider.IsAiming`（延迟获取 + 缓存）
  ——相机不再关心武器类型，**持近战自然不拉近**。
- 上半身姿态：`UpperBodyMode` 加 `RangedAiming` + `RangedAimingState`；`TryPlayAction` 闸门由 `!= RangedReady` 放宽为
  `!IsRangedMode()`（未瞄准/瞄准都算持枪，都能开火换弹）；`SetUpperBodyMode` 不再是空实现，改写真 `IsAiming (Bool)`，
  并用 `_currentMode` 缓存 + `WriteAimParameter()` 在 `Start`/`ApplyAnimationSet` 后各重发一次（换 controller 会重置参数）。
  `CharacterRoot.UpdateAimState` 里 `upperBodySM.SetMode(...)`（内部已去重），`GetUpperBodyMode` 改实例方法读 `IsAiming`。
- **关键技术约束**：枪械的「未瞄准套 ↔ 瞄准套」**不能**用第三个 `RuntimeAnimatorController` 整体切——
  运行时给 `Animator.runtimeAnimatorController` 赋值会重置状态机（跳回默认态、丢 Trigger），按住/松开瞄准键会反复打断攻击与受击动画；
  必须在**同一个枪械 controller 内部**用 `IsAiming` 切。

**验证结果**

- `static_check.ps1` PASS（pass=4 fail=0 skip=0）；本次改动的代码文件诊断 0。
- `PlayerInputActions.inputactions` 经 `ConvertFrom-Json` 校验语法合法；`PlayerInputActions.cs` 的 8 个注入点逐条核对齐全
  （动作/绑定 JSON、FindAction、字段、属性、Add/RemoveCallbacks、接口）。
- 新脚本 `IAimStateProvider.cs` 的 meta 以既有 meta 为模板改 GUID，格式字节级一致、全项目新旧 GUID 各命中 1 处。
- 动画链路自检：`UpperBodyMode` 三态与 `ApplyMode` 分支一一对应；`TryPlayAction` 闸门覆盖到 `RangedAiming`（否则瞄准中开不了火）；
  `SetUpperBodyMode` 的全部调用路径都能写到 Animator。

**待办（Unity 侧）**

- Unity 会重新导入 `.inputactions` 并重新生成 `PlayerInputActions.cs`；若生成结果与手改版有 diff，以编辑器生成为准。
- 手感调参：`CameraFollow.aimDistance` / `aimSmoothTime`、「开火自动瞄准」的尾巴 `CharacterConfig.aimAutoHoldSeconds`
  （默认 1.5，已有 `Player.asset` 自动取代码默认值）。
- 入库：`Assets/Scripts/Role/Core/IAimStateProvider.cs` 与 `IAimStateProvider.cs.meta` 仍是 untracked
  （`git add -u` 不会带上，需显式 `git add`）。

**追加：多武器 → 多动作的整体方案**

背景（用户补充）：动画资源分三套——近战（刀）/ 枪械未瞄准 / 枪械瞄准。目标是「用不同武器时播对应动作」，
且玩家开局默认持近战武器。

设计：把「哪把武器播哪套动作」拆成**两条正交轴**，两者的切换代价差一个数量级，必须用不同手段：

| 轴 | 触发频率 | 手段 | 是否重置状态机 |
| --- | --- | --- | --- |
| 武器类型（刀 ↔ 枪） | 低（拾取 / 切槽） | 整体替换 `RuntimeAnimatorController` | 会，可接受 |
| 枪械姿态（未瞄准 ↔ 瞄准） | 高（按住 / 松开右键） | 同一 controller 内 `IsAiming` 参数 | 不会 |

关键结论：因为瞄准轴不能重置状态机，**「枪械瞄准套 / 未瞄准套」必须共处同一个 controller**，
不能拆成第三个 controller 整体切——这决定了「枪械 controller」这个资产本身要能表达两套姿势。

本次落地（代码）：

- `WeaponConfig`：新增 `animatorController`（表现层字段）——由武器自己声明用哪套动画集，
  **新增武器只需 Create 一个 SO 填引用，零代码改动**；
  同时删掉死字段 `animPoseParam`（早期「用 Animator 参数表达姿态」的残留，全项目无人读取，已被整体切 controller 取代）。
- `CharacterAnimationController`：新增 `ResolveController()`——武器自身配置优先，
  未配置按 `type` 回退到 Inspector 上的 `meleeController` / `rangedController`（保留向后兼容）；
  无武器时返回 null、保持现状。
- `CharacterConfig`：新增 `initialWeapons` 数组——**玩家开局默认装备**（各按自己的 slot 落槽，
  数组最后一个成为手持武器）。此前这条链路只对敌人生效（`EnemyBrain.Start` 调 `Pickup(config.meleeWeapon)`），
  玩家侧完全缺失，属于「出生空手」的根因。
- `CharacterRoot.Start`：新增 `EquipInitialWeapons()`，复用 `Pickup` 全链路（落槽 → 切换 → 广播 `WeaponEquipped`），
  动画集 / 上半身姿态 / 武器模型都走正常流程，不另开旁路。
  顺带修掉上一轮引入的重复 XML 注释。

---

### 2026-09-10（续，跨零点）｜瞄准 strafe 转向 + 动画资源形态 + 换装动画（Put / Take）

**资源形态（用户确认）**：未瞄准套是 **1D**（只有前后，根本没有 8 方向资源）；瞄准套是 **2D 七位置**
（`R_AimIdle`(0,0) / F(0,1) / FL(-1,1) / FR(1,1) / B(0,-1) / BL(-1,-1) / BR(1,-1)，**没有纯左右** `(±1,0)`，侧移由相邻格位插值）。

**根因**：即便 7 位置 Blend Tree 搭好也不会生效——`WalkState`/`RunState` 里的 `character.RotateToward(dir)`
让角色**朝移动方向转身**，转完后 `transform.InverseTransformDirection(worldDir)` 恒为 `(0, 0, 1)`、`MoveX` 永远是 0，
四个斜向格位走不到。而 `IInputProvider.MoveDirection` **本来就是相机相对方向**（`PlayerInputProvider` 按 `Camera.main` 投影算），
所以解法不是转换输入空间，而是**把朝向从「移动方向」改成「相机方向」**，本地分解天然就是 横移(MoveX) / 前后(MoveY)。

**改动（代码）**

- `CharacterRoot`：`RotateToward` 拆出带角速度的私有重载（public 版保留给 `EnemyBrain`）；新增
  `RotateByMovement()`（**瞄准时直接 return**，不朝移动方向转）与 `UpdateFacing()`（瞄准时朝 `LookDirection`
  的水平投影转，角速度 `config.aimRotationSpeed`）；`UpdateFacing()` 放在 `canMove` 块内、`fullBodySM.OnUpdate()` **之前**
  ——Idle/Walk/Run/Jump/Fall 全都能锁朝向，死亡/眩晕/UI 模态时又不会转。
- `WalkState` / `RunState`：`RotateToward(dir)` → `RotateByMovement(dir)`。
- `CharacterConfig`：新增 `aimRotationSpeed`（默认 30；`rotationSpeed` 是 12，不够跟手）。
- 顺带：**瞄准时禁跑**（`SprintPressed && !IsAiming`，`RunState` 进瞄准退回 Walk）——瞄准套只有走路动画，跑必滑步。
  若想保留 ADS 跑动，删掉这两处判断即可。

**验证结果**

- `static_check.ps1` PASS（pass=5 fail=0 skip=0，含新加的绑定路径规则）；改动文件诊断 0；`git diff --check` 干净。
- `run_check.ps1`：静态 PASS；**编译 / 场景审计被脚本主动中止**——检测到编辑器正开着本项目
  （保护设计，未 `-Force` 强跑）。待关掉编辑器后补跑。
- 资源侧现状（用户已搭好）：枪械 controller `RangedLocomotion` + `RangedAimLocomotion`（未瞄准 1D / 瞄准 2D，
  `IsAiming` 双向过渡、关 Exit Time），并含 `Put` / `Take`。

**待办（Unity 侧）**

- **多插槽武器系统**：见下方专条。
- 手感调参（非阻塞）：`CharacterConfig.aimRotationSpeed`（默认 30）、`EquipmentController.putDuration`(0.55) / `takeDuration`(0.75)。

**待办专条 · 多插槽武器系统（手 / 背后 / 附加）**

资产包的动画里本来就带 `SwitchSocket` 事件（43 个剪辑里都有），原设计是「手上 / 背后 / 附加」三类插槽；
**我们目前只做了「单一手持模型」**，所以「收刀到背后」这类动作演不出来：

- 代码现状：`DispatchSocketCommand` 里只有 `IK_ON/OFF_Left_Handle` 真正生效；`To_Hand_R_Socket` 是显式空操作，
  `To_Put_Socket_Rifle` / `To_add_weapon_r` 被静默忽略 ⇒ 缺的是插槽逻辑本身，不是动画事件。
- 模型现状：角色模型**自带** `Weapon_Katana_mini_R` / `Weapon_Katana_mini_L` 两个刀节点（资产包原始结构，挂在手骨上），
  代码 `RefreshWeaponModel` 只销毁/生成自己 `Instantiate` 的那一份、管不到它们 ⇒「拿到枪后刀还在手上」不是切换逻辑的问题。
  （副作用：手持刀时是「自带节点 + `Weapon_Saw.modelPrefab` 实例」两份重叠，位置几乎重合、肉眼难辨。）
- 待做：
  1. 自带刀节点（R/L）改成运行时查找 + 按槽位开关，或干脆让手持模型复用它们、不再 `Instantiate`。
  2. 自建背挂父节点（背后挂点），在 `DispatchSocketCommand` 里补 `To_Put_Socket_Rifle` / `To_add_weapon_r` 分支。
  3. 收刀 / 挂枪的插槽坐标与姿势对齐（事件已就位，缺挂点与插槽逻辑）。

**注意**：`IsAiming` 的「开火后自动保持」也作用于 AI（`HandleAttackCommitted` 会给 `_autoAimRemain`）。
当前敌人只用近战（`EnemyConfig.meleeWeapon`）故无影响；将来给敌人配枪时需复核 strafe 与禁跑是否合适。

**换装动画（Put / Take）落地**

- 两个 controller 各加 `Put` / `Take` 状态：参数 `PutWeapon` / `TakeWeapon`（Trigger）+ `EquipSpeed`（Float）；
  剪辑近战 `TK_Put`/`TK_Take`、枪械 `PutGun`/`TakeGun`。两状态各有 `Hit`/`Die` 转出（换装必须可被打断）；
  枪械侧从 `RangedLocomotion` **与** `RangedAimLocomotion` 都能转入——瞄准中换枪不接就会 Trigger 排队卡死。
- `EquipmentController`：`enum SwitchPhase { None, Put, Take }` + 单计时器，`putDuration`=0.55 / `takeDuration`=0.75
  开放到 Inspector；`Drop()` 共用 `ApplyEquipped`（丢枪自动也带 Take）；删掉 `SWITCH_DURATION = 0.3f`。
- `CharacterAnimationController`：Put 在**旧**动画集上播（新实例事件 `UnequipStarted`），落位时才换 controller。
  **换 `runtimeAnimatorController` 会重置状态机 → 必须 `ApplyAnimationSet` 之后才 `SetTrigger`**，先设会丢。
  `PlayEquipAnimation` 用 `clip.length / 逻辑时长` 反算 `EquipSpeed`（Clamp 0.5~6）对齐动画与逻辑，
  避免「动画没演完就能攻击」与「攻击 Trigger 在 Take 里排队、回待机才突然挥一刀」两种错帧；调手感只调那两个时长。
- **固有限制**：Put 与 Take 跑在两个不同 controller 上，「收完武器 → 新集待机」之间**必然是硬切**，无法跨 controller 淡入淡出。
- `RefreshWeaponModel` 改为**只归零 position**、保留预制体自身旋转/缩放（刀根节点带 180° 绕 X 摆正刀刃，强制 identity 会躺倒）
  ——约定：武器朝向自己摆好，代码不覆盖。坑：`Weapon_Saw.asset` 的 `modelPrefab` 一直是 `{fileID: 0}`（**资产建了 ≠ 挂上了**）。
- 切槽的静默出口补提示：`EquipmentController.GetWeaponInSlot()` 查询 API + 三槽占用日志/Toast
  （空槽「「主武器」槽位没有武器」、同槽「已经拿着「X」」）——这两种「合法静默」以前与输入失灵表现完全一样。
- **输入绑定修正**：`<Keyboard>/digit1|2|3` 是**死路径**——布局里主键盘数字键的控件名就是 `"1"/"2"/"3"`，
  `Keyboard.current.digit1` 只是 C# 属性名，**绑定路径解析的是布局名**。已改为 `<Keyboard>/1|2|3`，并加绑小键盘 `numpad1|2|3`；
  同时新增 FAIL 级审计规则 `Test-InputBindingPaths`（拿包缓存 `Keyboard.cs` 的 115 个控件名校验全部 `<Keyboard>/xxx`，死路径直接拦下）
  ——「某个键完全没反应、别的键都好」先怀疑路径解析。

**下一步**

- 准星 UI；瞄准时移动降速（若需要）。

---

## 第三部分 · 下次开发计划

### 当前架构总览

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| FullBody 状态机 | 完成 | Idle/Walk/Run/Jump/Fall；后续供 AIInputProvider 复用 |
| UpperBody 状态机 | 完成 | Inactive/RangedReady/RangedAiming + Fire/Reload/UseItem 语义；Sink 已下发 `IsAiming (Bool)` |
| 动画集切换 | 完成 | `WeaponConfig.animatorController` 数据驱动（武器自身优先，按 `type` 回退）；枪械 controller 两套平行 locomotion（未瞄准 1D / 瞄准 2D）+ `Put`/`Take` 换装 |
| 相机系统 | 完成 | 跟随 + 鼠标旋转 + 瞄准状态平滑拉近（只改距离，不动高度） |
| 瞄准状态 | 完成 | `IAimStateProvider` + `CharacterRoot.IsAiming`：持远程武器 且（右键按住 或 开火后自动保持期内）；瞄准时 strafe 转向锁相机方向 |
| CharacterConfig | 完成 | ScriptableObject 配置（移动 / 跳跃 / 转身 / 瞄准自动保持时长与转向速度 / 初始装备） |
| Blackboard | 已下线 | 已改为角色实例级 CharacterContext（聚合 Runtime + CombatStats），静态全局容器删除 |
| CharacterContext | 完成 | 角色共享上下文：聚合 Runtime + CombatStats，由 CharacterRoot 组装注入，收敛根节点字段暴露 |
| EventBus + Coordinator | 完成 | 全局事件通知 + 角色强状态协调；AI 复用 Coordinator 的 Dead/Stunned 等状态 |
| PlayerInputProvider | 完成 | 移动/视角/跳跃/冲刺/E/UI；鼠标左键攻击，右键瞄准（AimHeld），数字 1/2/3 选武器槽，G 丢弃武器 |
| 交互系统 | 完成 | IInteractable + Detector + PickupItem/WeaponPickupItem + PromptUI |
| 全局提示 (ToastView) | 完成 | UI_Toast 事件驱动，淡入/上飘/淡出，任意模块可触发 |
| UI 模态 | 完成 | 仅 IsPlayerControlled 角色订阅 UI_ModalChanged，敌人不受玩家开面板影响 |
| EquipmentController | 逻辑层完成 | 三槽切换、每槽冷却、全/半自动、地图拾取、丢弃；可作为攻击执行层复用 |
| 武器插槽系统（手/背/附加） | 未开工 | 资产包原设计三类插槽（剪辑里都带 `SwitchSocket` 事件）；代码目前只做手持，`DispatchSocketCommand` 里仅 `IK_*_Left_Handle` 生效 |
| Combat 模块 | 完成 | 武器策略、加成、命中 + 正式 Health/Faction/DamageContext 双向伤害死亡闭环 |
| 敌人 AI | A* 阶段完成 | EnemyBrain(Idle/Chase/Attack/Dead) + AIInputProvider + 感知；导航经 IEnemyNavigation 解耦 |
| A* 导航 | 完成待 Unity 对接 | 2.5D 物理采样网格 + A* + 路径跟随/重算节流/失败与卡住诊断/Gizmos |
| IKController | Stub | 手脚 IK，待战斗系统激活 |
| ExpressionController | Stub | 面部表情 |
| AudioController | Stub | 已加 PlayFootSound 空方法 |

### 建议开发优先级

| 优先级 | 模块 | 说明 |
| --- | --- | --- |
| 1 | ~~今晚 Unity 对接~~ | 已完成（见 09-07 记录） |
| 2 | ~~多角色安全基础~~ | 已完成（见 09-08 记录） |
| 3 | ~~敌人 AI 垂直切片~~ | 已完成（见 09-09 记录） |
| 4 | ~~A* 导航~~ | 已完成（见 09-10 记录：Unity 对接通过） |
| 5 | AI 战术亮点 | Patrol/Investigate/ReturnHome + Utility AI + Influence Map + Context Steering/RVO |
| 6 | 武器与 Animator 表现层 | 进行中：动画集切换 + strafe 转向 + 换装 Put/Take 已通；待准星 UI、锯子全身攻击 |
| 7 | 多插槽武器系统（手 / 背后 / 附加） | 未开工：接管模型自带的刀节点 + 自建背挂点 + 补 `SwitchSocket` 插槽分支（详见 09-10 待办专条） |

> A* 导航（第三阶段）已完成并过了 Unity 对接（见 09-10 记录）。下一步进入 Patrol/Investigate/ReturnHome、Utility AI、Influence Map 与局部避障。

---

## 附录 · 每日记录模板

复制下面这段，把日期和标题改成当天的，追加到「第二部分 · 按日期日志」末尾。

```markdown
### YYYY-MM-DD｜当天主题

**今日完成（代码）**
- 

**待办（Unity 侧）**
-

**验证结果**
- ✅ 已跑通：
- ⚠️ 待改：
- 🐛 踩的坑：

**下一步**
- 
```
